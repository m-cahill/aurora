# M37 — Developer experience and local CI parity helpers

**Timeframe:** Merged to `main` 2026-05-06 (PR #48, merge commit `acfabb13…`); documentation-chain and ledger-sync commits on `main` (see ledger).

## Summary

M37 gave contributors **`Makefile`** targets that mirror **`ci` / `repo-safety`** (verifier, compile, **`unittest`**, branch coverage + **`artifacts/coverage.json`**, **Mypy**, **Ruff**, **`pip-audit`**) via **`make ci-local`**, updated **`DEVELOPMENT.md`** (*Local CI parity*), **`AURORA_OPERATING_MANUAL.md`**, and **`README.md`**, and added an optional **stdlib** **`scripts/check_api_docs_exports.py`** plus **`tests/test_check_api_docs_exports.py`**—**not** wired into GitHub Actions. **Explicit caveat:** local/global **`pip-audit`** noise is **not** authoritative; **clean CI** is.

## What changed

- **`Makefile`**, DX docs, optional export-heading checker and tests.

## What it proved

- Day-to-day workflows could **approximate** enforced CI locally without changing **`src/aurora/`** or workflow YAML.

## What it did not prove

- Performance baselines, SBOM artifacts, release lock, or any **native**/**decode**/**graph** claims.

## Key deliverables

- **`make ci-local`** and documented parity expectations; optional API-doc hygiene script.

## Evidence (public)

- PR #48; post-merge and chain-tip CI runs in **Milestone M37** ([`aurora.md`](../aurora.md)).

## Why it mattered

Friction slows governance-heavy repos; M37 reduced **surprise** between laptop and Actions while keeping **authority** on GitHub.
