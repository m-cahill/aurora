# M05 — Provenance-preserving runtime substrate establishment and Phase A closeout gate

**Timeframe:** Initialized 2026-03-26; merged to `main` 2026-03-27 (PR #6, merge commit `04a5527…`).

## Summary

M05 created the **first-party Python package** at `src/aurora/` with minimal **substrate metadata** (`RuntimeSubstrateMetadata` in `surface.py`), [`docs/runtime_substrate.md`](../runtime_substrate.md), and CI using **`PYTHONPATH=src`** for tests. **Phase A — Governance and Safety** closed; **Phase B** authorized for seam work. No `mediapipe` imports under `src/aurora/` (later enforced by verifier and tests).

## What changed

- `src/aurora/__init__.py`, `src/aurora/runtime/__init__.py`, `surface.py`.
- Substrate tests and verifier gates for tracked files.
- `DEVELOPMENT.md` updated for import instructions.

## What it proved

- The repo contains an **importable** first-party package boundary separate from upstream clones.
- CI could exercise **import and metadata** truthfully.

## What it did not prove

- No dispatcher implementation, no `AuroraImage`, no native loading semantics.

## Key deliverables

- `docs/runtime_substrate.md`, `src/aurora/` package skeleton.

## Evidence (public)

- PR #6; post-merge `main` CI success per ledger.

## Why it mattered

All later seam and ARB work **anchors** to this package root. M05 chose **`src/aurora/`** layout explicitly and documented provenance rules.
