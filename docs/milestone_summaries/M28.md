# M28 — ARB v0.1 minimal reader

**Timeframe:** Initialized 2026-04-03; merged to `main` 2026-04-03 (PR #29, merge `15c17a1…`).

## Summary

M28 added **`read_arb`** and **`ArbBundle`** — eager load of the minimal v0.1 tree with **`manifest.json`** validation matching **`write_arb`**. **No implicit hash verification** in the reader API (validation is separate — M29).

## What changed

- `reader.py`, tests, exports, verifier list.

## What it proved

- Round-trip **read after write** works for minimal valid bundles; error paths for missing files and malformed inputs.

## What it did not prove

- Cryptographic integrity unless **`validate_arb`** is called (M29).

## Key deliverables

- `ArbBundle` dataclass, `read_arb`.

## Evidence (public)

- PR #29; CI green.

## Why it mattered

**Load ≠ verify** is an important API distinction for downstream tooling; M28 kept that clean.
