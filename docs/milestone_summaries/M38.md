# M38 — Bounded ARB performance baselines, SBOM emission, and release evidence posture

**Timeframe:** Merged to `main` 2026-05-06 (PR #50, merge commit `0cdb42f8…`); project-record close commits on `main` (see ledger).

## Summary

M38 added **evidence-only** machinery: stdlib **`scripts/measure_arb_performance.py`** and tests, **[`arb_performance_baseline.md`](../arb_performance_baseline.md)** (**`non_sla`** methodology), **[`release_evidence.md`](../release_evidence.md)** (how CI artifacts support release narrative), **`Makefile`** targets **`performance`**, **`sbom`**, **`release-evidence`**, and **additive** **`ci`** steps uploading **`artifacts/arb_performance_baseline.json`** and **`artifacts/sbom.cdx.json`** after **`pip-audit`** (pinned **CycloneDX** via **`pip-audit`**—**no** new SBOM-only dependencies). **No** `src/aurora/runtime/` or **`arb`** behavior changes; **no** **`requirements-*`** / **`pyproject.toml`** edits for M38 itself.

## What changed

- Measurement script + docs; CI artifact uploads; DX doc pointers; M37 record parity carry-forward in **`aurora.md`** where noted.

## What it proved

- The repo could publish **repeatable** timing JSON and a **clean-environment** SBOM-shaped artifact **without** turning them into enforcement gates on business logic.

## What it did not prove

- SLA guarantees, native/MediaPipe performance, or correctness of decode/graph/TFLite paths.

## Key deliverables

- **`non_sla`** baseline doc + JSON; SBOM JSON from audited env; **`release_evidence.md`** tying artifacts to release hygiene.

## Evidence (public)

- PR #50; run IDs and job IDs in **Milestone M38** ([`aurora.md`](../aurora.md)).

## Why it mattered

Releases need **documented** evidence trails; M38 added bounded, honest artifacts **ahead of** the final tag (**M39**).
