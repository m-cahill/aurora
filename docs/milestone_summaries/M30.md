# M30 — ARB v0.1 minimal CLI validator

**Timeframe:** Initialized 2026-04-03; merged to `main` 2026-04-04 (PR #31, merge `b6378a8…`).

## Summary

M30 added **`src/aurora/arb/__main__.py`**: **`python -m aurora.arb <bundle-root>`** calls **`validate_arb`** only. **`sys.argv`** contract documented: **two** elements for a normal `-m` run (**no** `-m` / module name in argv). Exits **0** valid, **1** validation failure, **2** usage error. Fixed **`sys.argv`** semantics after first implementation mismatch.

## What changed

- `__main__.py`, `test_arb_cli.py`, verifier required files, spec §7.2.

## What it proved

- Users can validate bundles **without** writing Python glue for the common case.
- CLI behavior is **test-covered** and consistent with the spec’s argv story.

## What it did not prove

- Inspect/read subcommands, replay, or runtime seam changes.

## Key deliverables

- Validate-only CLI entry point for ARB v0.1.

## Evidence (public)

- PR #31; CI green including `__main__.py` coverage.

## Why it mattered

Operational adoption often starts at the **CLI**; M30 made validation a **one-liner** for integrators.
