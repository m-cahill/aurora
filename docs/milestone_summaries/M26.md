# M26 — ARB v0.1 canonical format and deterministic hashing contract

**Timeframe:** Initialized 2026-04-02; merged to `main` 2026-04-02 (PR #27, merge `67f3ebb…`).

## Summary

M26 added **`docs/aurora_run_bundle_v0_spec.md`**: normative **v0.1** on-disk layout, **`arb_version`: `"0.1"`**, canonical JSON rules, SHA-256 manifest and root hash rules, **offline/batch** scope; deferred LIVE_STREAM/training profiles. Verifier requires the spec file. **No** `src/aurora/` implementation.

## What changed

- Spec document; cross-links; `DEVELOPMENT.md` alignment.

## What it proved

- The repo could publish a **deterministic** ARB v0.1 contract **before** writer/reader/validator shipped.

## What it did not prove

- That implementations exist yet (M27+).

## Key deliverables

- `aurora_run_bundle_v0_spec.md` as the ARB v0.1 law for implementations.

## Evidence (public)

- PR #27; CI success.

## Why it mattered

Implementations without a spec drift; M26 made **bytes-on-disk** the contract.
