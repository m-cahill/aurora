# M20 — Audio classifier graph mapping and native-wiring target lock

**Timeframe:** Initialized 2026-03-31; merged to `main` 2026-04-01 (PR #21, merge `c7e9de3…`).

## Summary

M20 added **`docs/audio_classifier_graph_mapping.md`**: evidence-backed map from Python Tasks audio entry points through C API to C++ **`AudioClassifierGraph`**, including **`AudioToTensorCalculator`** and inference/post-processing notes. It documents **BUILD vs graph `.cc`** nuance for framer calculators — **no** claim that a dedicated framer **node** appears in `audio_classifier_graph.cc` where the graph composes **`AudioToTensorCalculator`** directly.

## What changed

- Graph mapping doc; updates to inventory and kernel strategy; `aurora.md` pointers.

## What it proved

- The repo could **ground** future D1 wiring targets in **observable** upstream structure (via read-only clone study).

## What it did not prove

- Runtime correctness, decode, or that any particular native path works in CI.

## Key deliverables

- `audio_classifier_graph_mapping.md` as scope-lock for M21+.

## Evidence (public)

- PR #21; CI green.

## Why it mattered

Native wiring without a map invites **false assumptions** (e.g., framer placement). M20 locked **proof obligations vs non-goals**.
