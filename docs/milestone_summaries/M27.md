# M27 — ARB v0.1 minimal writer and hash utilities

**Timeframe:** Initialized 2026-04-03; merged to `main` 2026-04-03 (PR #28, merge `1c8e135…`).

## Summary

M27 delivered the **`src/aurora/arb/`** package foundation: **`canonicalize`** (no JSON floats per spec), **`sha256_hex`**, **`compute_root_hash`**, and **`write_arb`** producing a **minimal valid** v0.1 tree. Tests pin **§8** illustrative digests. **Stdlib only**; **no** `src/aurora/runtime/` changes.

## What changed

- `canonical_json.py`, `hasher.py`, `writer.py`, `tests/test_arb_*.py`, package `__init__.py`, verifier `ARB_V0_REQUIRED_FILES`.

## What it proved

- The repo can **emit** a spec-conformant bundle and **reproduce** canonical hashes for the reference example.

## What it did not prove

- Reader, validator, CLI, replay, or semantic validation in CI beyond structural checks.

## Key deliverables

- `write_arb` and hashing utilities aligned with `aurora_run_bundle_v0_spec.md`.

## Evidence (public)

- PR #28; CI `repo-safety` including coverage on new code.

## Why it mattered

Write path is the **first** executable ARB story — it validates the spec **in code**.
