# M31 — AURORA v0.1 project closeout and downstream handoff record

**Timeframe:** Initialized 2026-04-04; merged to `main` 2026-04-04 (PR #32 on GitHub, merge `383a0b6…`).

## Summary

M31 was **docs/governance only**: recorded **v0.1 complete**, **maintenance-only** default for runtime scope, **open core** vs downstream layers, **public handoff surface** listing (`aurora.runtime`, `aurora.arb`, committed docs), long-horizon roadmap **reconciliation** (table vs actual ledger execution), and **deferred-topic buckets**. **No** `src/aurora/` changes; **no** workflow YAML edits; **no** downstream repo creation.

## What changed

- `docs/aurora.md` project status, boundaries, and pointers (including **M32–M35** public-release hardening tranche authorization).

## What it proved

- The program could **close** a coherent v0.1 narrative **without** inventing new runtime proof.
- Handoff language for ORNITHOS/PANTANAL-1 consumers is **explicit** about non-overlap and one-way dependencies.

## What it did not prove

- Any new native, decode, graph, or competition behavior.

## Key deliverables

- v0.1 completion record; maintenance posture; M32 pointer for follow-on work.

## Evidence (public)

- PR #32; post-merge `main` CI success IDs in ledger; enumeration commits on `main` for doc chain.

## Why it mattered

Programs fail when “done” is undefined. M31 defined **v0.1 done** as **bounded substrate + ARB v0.1 + truthful CI**, with **honest** non-proofs preserved for what comes next.
