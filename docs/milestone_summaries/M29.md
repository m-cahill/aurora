# M29 — ARB v0.1 standalone validator

**Timeframe:** Initialized 2026-04-03; merged to `main` 2026-04-03 (PR #30, merge `e626baf…`).

## Summary

M29 implemented **`validate_arb`** and **`ArbValidationError`**: re-hash on-disk bytes, check manifest and canonical JSON rules, verify **`sha256_manifest.json`** structure and **`root_hash.txt`** against manifest bytes per spec. **`read_arb`** unchanged. First PR iteration failed coverage on `validator.py`; fixed before merge.

## What changed

- `validator.py`, large test file, exports, spec §7.1 alignment.

## What it proved

- Integrity and minimal semantic consistency can be checked **independently** of loading.
- Coverage discipline extends to new ARB modules (**100%** line/branch maintained).

## What it did not prove

- Replay tooling, transport, or CLI (M30 added validate-only CLI).

## Key deliverables

- `validate_arb` as the integrity entry point for bundles.

## Evidence (public)

- PR #30; CI runs recorded; merge SHA in ledger.

## Why it mattered

External certifiers and downstream CI need a **single** validation primitive — M29 provided it.
