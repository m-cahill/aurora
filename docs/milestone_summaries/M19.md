# M19 — Bounded Python-first audio seam (Candidate A — AuroraAudio)

**Timeframe:** Initialized 2026-03-31; merged to `main` 2026-04-01 (PR #20, merge `4ee7570…`).

## Summary

M19 implemented **`AuroraAudio`** and **`AudioCreationError`**, parallel to **`AuroraImage`**, with **`audio_dispatch.py`** and **`AUDIO_FROM_FILE` / `AUDIO_FROM_BYTES`** tokens in **`dispatch_tokens.py`**. Tests and smoke composition updated. **No** C++/BUILD, **no** `mediapipe/` copy, **no** workflow identity change.

## What changed

- `audio.py`, `audio_dispatch.py`, `dispatch_tokens.py`, tests including smoke.

## What it proved

- A **second** bounded domain seam could reuse the same **injection** model as image.
- Structural tests with fakes cover the Python contract.

## What it did not prove

- Decode correctness, acoustic kernel extraction, or `@com_google_audio_tools` in-repo.

## Key deliverables

- `AuroraAudio`, audio dispatch helpers, M19 documentation in `DEVELOPMENT.md`.

## Evidence (public)

- PR #20; CI `repo-safety` success.

## Why it mattered

Audio is the program’s long-term focus; M19 made the **Python seam** real before native wiring (M21–M22).
