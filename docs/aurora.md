# AURORA — Project Record

**Canonical anchor:** AURORA is the governed acoustic runtime and MediaPipe refactor program tracked in this repository; it is a Foundry-aligned case study and the substrate for PANTANAL-1, aimed at deterministic, artifact-bound, replayable audio execution without pretending the upstream monorepo is already well-governed.

**Status:** Active — **Phase A — Governance and Safety** is **closed**; **Phase B — Runtime Seam Normalization** is **closed** at **M10**; **Phase C — Abstraction and Deduplication** is **closed** at **M16**; **Phase D — Kernel Extraction** is **closed** at **M23** (**Phase D governance closeout** — **not** monolithic kernel extraction, **not** decode/graph/parity proof): **M17**–**M22** delivered **bounded** ingress documentation + **structural** Python + ctypes seams for **`AUDIO_FROM_FILE`** and **`AUDIO_FROM_BYTES`**; **local-native** proof was **not attempted** — documented **external** / out-of-scope absence (workspace **`docs/milestones/M22/M22_local_native_proof.md`**), **not** a hidden defect in **`aurora/`**; **no** CI-native real-library execution, **no** decode correctness, **no** graph/TFLite correctness, **no** MediaPipe application parity — see **Phase D closure criteria (recorded at closeout)** below; next milestone: **M26** (**stub** — workspace **`docs/milestones/M26/`**); **M25** (**ARB boundary framing and minimal run-bundle specification**) — **complete** on **`main`**: **PR** **#26** — https://github.com/m-cahill/aurora/pull/26 — **`gh pr merge 26 --merge --admin`**. **Merge commit:** **`4037db3b0c037efea533e1a353565c9f70139a14`** (**2026-04-02T02:57:26Z** UTC). **Governance-only** — **no** `src/aurora/` runtime change; **no** new row in **Execution Phase Boundaries (locked)**; **post–Phase D** execution-phase name remains **unset** (see **`docs/aurora_run_bundle_boundary.md`** §8). **M24** (**Phase E readiness audit + ecosystem boundary charter**) — **complete** on **`main`**: **PR** **#25** — https://github.com/m-cahill/aurora/pull/25 — **`gh pr merge 25 --merge --admin`**. **Merge commit:** **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`** (**2026-04-02T01:41:38Z** UTC). **Governance-only** — **no** `mediapipe/` modification; **no** ORNITHOS/PANTANAL-1 repo creation; **Proposed downstream repo layout and dependency directionality** is **boundary guidance** — **not** proof that **`ornithos/`** / **`pantanal-1/`** repositories exist. **Does not prove:** decode, graph, parity, native execution, competition readiness, or downstream repo implementation.  
**Last updated:** **2026-04-02** — **M25** (**ARB boundary framing** — **`docs/aurora_run_bundle_boundary.md`**) **merged** to **`main`**: **PR** **#26** — https://github.com/m-cahill/aurora/pull/26 — **`gh pr merge 26 --merge --admin`**. **Merge commit:** **`4037db3b0c037efea533e1a353565c9f70139a14`** (**2026-04-02T02:57:26Z** UTC). **Final pre-merge PR head:** **`6eeac778651e5cd7c92d4d97f526ee8cbca186a8`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23881492177`** (`push`, **success**), **`23881499188`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23881508861`** (**success**, head **`4037db3b0c037efea533e1a353565c9f70139a14`**). **Authoritative `main` tip immediately after merge (merge commit):** **`4037db3b0c037efea533e1a353565c9f70139a14`**. **M25** **closeout project record on `main`:** **`12fac02dcd9e88fea72e9c5e3da11c3da585b12b`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23881539057`** (**success**). **Authoritative final enumerated `main` tip (M25 closeout project record):** **`12fac02dcd9e88fea72e9c5e3da11c3da585b12b`**. **Prior — M24** (**2026-04-02**): **M24** (**Phase E readiness audit** + **ecosystem boundary charter**) **merged** to **`main`**: **PR** **#25** — https://github.com/m-cahill/aurora/pull/25 — **`gh pr merge 25 --merge --admin`**. **Merge commit:** **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`** (**2026-04-02T01:41:38Z** UTC). **Final pre-merge PR head:** **`f9d9fbe3603d10402023b42896efb9d96e10e4d0`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23879281514`** (`push`, **success**), **`23879282695`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23879539276`** (**success**, head **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`**). **Authoritative `main` tip immediately after merge (merge commit):** **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`**. **M24** **closeout project record on `main`:** **`21985bf488b67ccf4c62ba2fcaeac16c1d6429cd`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23879695944`** (**success**). **Authoritative final enumerated `main` tip (M24 closeout project record):** **`21985bf488b67ccf4c62ba2fcaeac16c1d6429cd`**. **M23** (**Phase D governance closeout**) baseline on **`main`**: **PR** **#24** — https://github.com/m-cahill/aurora/pull/24 — merge commit **`64cc610d690e61edb026c37ec7cd1915a1fbba89`**; **closeout project record** **`9d8296d449c42a43a505f539a0736a4b0a213de4`** — **`ci` / `repo-safety`:** **`23877506186`** (**success**). *(Full **M17**–**M25** milestone rows remain in the **ledger** below.)*

This file is the **authoritative project record** for the `aurora/` git repository. It is maintained at milestone boundaries (especially closeout) so the repo stays legible, bounded, and aligned with evidence from audits and preflight locks.

---

## What AURORA is

AURORA is:

- a **governed acoustic runtime** — explicit seams, honest async semantics, and traceable outputs over time
- a **MediaPipe refactor program** — behavior-preserving, evidence-based change on a real fork’s audio-capable surfaces (not a casual whole-repo rewrite)
- a **Foundry-aligned case study** — proves architectural discipline on a large, messy codebase; does not ship MediaPipe code into Foundry
- the **substrate for PANTANAL-1** — the BirdCLEF-facing deployment and observatory narrative sits on top of AURORA and the ORNITHOS model layer

AURORA is not a generic “rewrite MediaPipe” effort, not a backdoor to import upstream implementation into Foundry core, and not a demand that every research path be forced through TFLite on day one.

---

## Workspace and repository model

The workspace groups several directories; **only one is a git repository**:

| Location | Role |
|----------|------|
| **`aurora/`** (this repo) | **Tracked repository.** All commits, branches, and PRs target here only. |
| **`mediapipe/`** (sibling in the workspace) | **Read-only reference clone** for study and evidence. Do not modify. |
| **`docs/`** at workspace root | **Local governance** — milestones, prompts, preflight packs. Read and follow; **do not commit** these into `aurora/`. |

Additional rules:

- **No git repository** at the workspace root — the boundary model depends on a single repo under `aurora/`.
- **No import, copy, or paste** of MediaPipe source from `mediapipe/` into `aurora/`. Architectural learning is allowed; code transfer is not.

**Proposed downstream repos:** **`ornithos/`** and **`pantanal-1/`** are **not** yet separate git repositories in this workspace; when they exist, they are **siblings** of **`aurora/`** — see **Proposed downstream repo layout and dependency directionality** below.

---

## Relationship to Foundry

- **AURORA is a case study, not Foundry core.** It may fork and change its own implementation surface.
- **Foundry may learn from AURORA only at the architectural level** — patterns, seams, evidence of what works at scale.
- **No MediaPipe-derived implementation** should be copied into Foundry core. Anything that “graduates” must be re-derived clean-room, implementation-agnostic, and justified beyond a single codebase where possible.

AURORA should remain **explainable in Foundry terms** without becoming dependent on Foundry code.

---

## Relationship to PANTANAL-1, ORNITHOS, and BirdCLEF

Naming and roles:

| Name | Role |
|------|------|
| **AURORA** | Governed runtime and refactor program (this repo’s focus). |
| **ORNITHOS** | Domain model stack — training, scoring, representation learning on top of AURORA surfaces. |
| **PANTANAL-1** | End-to-end deployment, BirdCLEF submission, observatory-style narrative, case study. |
| **BirdCLEF** | Public benchmark and stress test — not the sole definition of success, but a protected proof path. |

**Protected competition rail:** A valid BirdCLEF submission path must remain possible throughout the refactor. Refactor milestones must not block the benchmark; baseline and submission safety are tracked as a **parallel rail** (see *Program Roadmap (proposed)* — C01–C05). Exact repo layout and integration for that rail are **deferred**; the requirement is planning-level until a later milestone locks structure.

---

## Proposed downstream repo layout and dependency directionality

AURORA is intended to stop at the **governed acoustic runtime** boundary. The **domain model stack** and the **final BirdCLEF-facing system** are expected to live in **separate** projects so that **competition-specific** implementation does not bleed back into the runtime refactor repository.

### Proposed workspace shape

```text
aurora-project/
  aurora/         # governed acoustic runtime and AURORA case-study repo (this git repository)
  ornithos/       # bioacoustic model / training repo (proposed — not created by M24)
  pantanal-1/     # BirdCLEF 2026 deployment / submission / observatory repo (proposed — not created by M24)
  mediapipe/      # read-only upstream reference clone
  docs/           # local governance layer (not committed into aurora/)
```

### Dependency directionality

The intended dependency direction is **one-way**:

- **AURORA** depends on neither **ORNITHOS** nor **PANTANAL-1**.
- **ORNITHOS** may depend on **released AURORA** runtime surfaces and artifacts.
- **PANTANAL-1** may depend on **released AURORA** runtime surfaces and **released ORNITHOS** model artifacts.
- **PANTANAL-1** must not become a **hidden** dependency of **AURORA**.
- **ORNITHOS** must not become a **hidden** dependency of **AURORA**.

### Ownership boundary

**AURORA** owns the **generic governed runtime substrate**: runtime seams, graph/runtime execution surfaces, deterministic offline replay, generic artifact/runtime boundaries, and runtime-level observability and policy hooks — **as authorized by milestone scope** (Phase D remains **structurally** bounded per the **closure matrix**).

**ORNITHOS** owns the **bioacoustic model stack**: training pipelines, representation learning, species-scoring heads, long-context modeling, and model export paths that **consume** AURORA surfaces rather than redefining them.

**PANTANAL-1** owns the **final BirdCLEF-facing system**: competition-safe inference, submission generation, observatory outputs, and public competition/deployment packaging.

### Explicit non-overlap rule

BirdCLEF-specific notebooks, submission packaging, competition-only postprocessing, species-specific competition tuning, Kaggle compliance code, and benchmark-only orchestration should **not** be added to **AURORA** by default. If a change is only needed because **BirdCLEF 2026** is **BirdCLEF 2026**, it belongs in **PANTANAL-1** unless it also improves the **generic** governed acoustic runtime.

---

## Audit-established starting facts

The following are **binding starting truths** from full-repo and finishing audits (see workspace `docs/context/` for full evidence):

1. **Governance lags architecture** — the codebase is worth saving, but repo safety (CI, pins, gates) must be repaired before deep runtime surgery.
2. **The dispatcher seam is real** — almost all Tasks API modules use `SerialDispatcher`; formalizing a `Dispatcher` boundary is feasible and low-risk at the typing layer once prerequisites are met.
3. **Vision task duplication is real** — large, repeated boilerplate; abstraction is justified after seams are normalized.
4. **Audio calculators are modular** — C++ audio calculators are testable, composable ingredients for a future acoustic kernel; they depend on `@com_google_audio_tools`, which must be pinned and treated as a first-class dependency.
5. **TFLite is embedded in graph build macros** — treat TFLite as a **core first-wave dependency**; do not attempt to decouple the whole graph build in early phases.
6. **`@com_google_audio_tools` is a real external dependency** — any extraction or build that touches audio DSP must account for it explicitly.
7. **LIVE_STREAM callback order is not the determinism contract** — offline/batch modes are the strong determinism core; live/async semantics must be documented honestly (timestamps and recorded events, not callback order alone).

---

## Startup invariants and verification posture (preflight)

The workspace **preflight pack** (`docs/preflight/aurora_startup_lock/`) locks **sequencing, boundaries, and proof**, not the contents of this file. Summarized expectations:

- **Behavior preservation is the default** for refactors unless a change is explicitly classified otherwise, with criteria, tests or artifacts, and rollback clarity.
- **CI must be truthful** when introduced: green means safe; no `continue-on-error` on enforcement gates; no placeholder tests; no fake coverage narratives; evidence (logs, artifacts) inspectable after the fact.
- **Invariant surfaces** (public task APIs, Python→C dispatch path, model loading, outputs, async semantics, graph init, audio structural parity with vision, global-state discipline) require explicit verification appropriate to the change — smoke tests, golden outputs where needed, and honest CI.

Detailed tables live in `AURORA_INVARIANTS.md`, `AURORA_RUNTIME_TRUTHS.md`, `AURORA_CI_TRUTHFULNESS.md`, and `AURORA_VERIFICATION_PLAN.md` in the preflight pack.

---

## Execution Phase Boundaries (locked)

These phases define **ordering and permission**, not dates. Work **must not** skip ahead in ways that violate this sequence. This section is **locked** for startup sequencing; it is not the long-range milestone map.

| Phase | Name | What it means |
|-------|------|----------------|
| **A** | **Governance and Safety** | Make the repo safe to change: truthful CI, pins, lint baseline, branch protection, hygiene, canonical docs. **No** runtime seam extraction, dispatcher migration, or task-base refactors yet. |
| **B** | **Runtime Seam Normalization** | Make implicit seams explicit: `Dispatcher` protocol, `image.py` migration off raw CDLL (behavior-preserving), loader/documentation of async semantics, smoke tests per domain. **No** `VisionTaskBase` / `AudioTaskBase`, no C API signature changes, no kernel extraction. |
| **C** | **Abstraction and Deduplication** | Reduce duplication: task bases, data extraction, logging hygiene, honest coverage thresholds when baselines exist. **No** acoustic kernel extraction as a monolithic rewrite; no premature ARB implementation. |
| **D** | **Kernel Extraction** | **Closed** at **M23** — bounded **ingress** + **structural** first-party audio seams (see **`docs/kernel_ingress_strategy.md`**, **`docs/acoustic_kernel_surface_inventory.md`**, **`docs/audio_classifier_graph_mapping.md`**; **M19**–**M22** code — **not** full kernel implementation, **not** decode/graph/parity proof, **not** local-native execution proof). |

**Explicitly deferred until the right phase:** ARB **implementation** (writer/reader/schema/hash, tooling — **M25** committed **`docs/aurora_run_bundle_boundary.md`** as **boundary/spec only**; **not** runtime code), ORNITHOS architecture, graph-spec-as-law implementation, observatory layer, certification/RediAI integration details, Legacy Solutions deprecation without parity and migration story, plugin systems.

**Protected parallel competition rail:** BirdCLEF baseline and submission viability stay a **parallel** concern; refactor work must not eliminate the ability to produce a valid submission candidate.

**Phase state:** **Phase A — Governance and Safety** is **closed**. **Phase B — Runtime Seam Normalization** is **closed** at **M10**. **Phase C — Abstraction and Deduplication** is **closed** at **M16**. **Phase D — Kernel Extraction** is **closed** at **M23** (**governance closeout** — committed record + explicit proved/not-proved language). **M17**–**M22** are **complete** on **`main`** (see ledger): **M17**–**M18** (**docs-only**), **M19** (**bounded Python-first audio seam**), **M20** (**docs scope-lock** — **`docs/audio_classifier_graph_mapping.md`**), **M21** / **M22** (**bounded D1** — **`NativeAudioDispatcher`** for **`AUDIO_FROM_FILE`** / **`AUDIO_FROM_BYTES`**). **M23** closes **Phase D** as **bounded structural ingress + seams** — **not** acoustic kernel **implementation** end-to-end, **not** MediaPipe parity, **not** local-native proof (see closure criteria). Broader calculator lift, C++/BUILD/graph work in **`aurora/`**, and monolithic rewrites remain **not** authorized by default until a later milestone locks them. **M11**–**M25** **complete** on **`main`** (see ledger); **M25** **merged** **PR** **#26** (**ARB** **boundary** **framing** — **`docs/aurora_run_bundle_boundary.md`**); **M24** **merged** **PR** **#25** (**Phase** **E** **readiness** **audit** + **ecosystem** **boundary** **charter** — see header); **M23** **merged** **PR** **#24** (**Phase** **D** **governance** **closeout**).

### Phase D entry constraints (historical — milestones M17–M22)

During Phase D execution, until a milestone **explicitly** locked implementation beyond **planning / ingress / scoping** (see **`docs/kernel_ingress_strategy.md`**), **except** where a milestone authorized a **named bounded slice** only (e.g. **M19** — **delivered** as **PR #20**):

- **Planning-first / ingress-first / scoped / non-monolithic** — no default blanket kernel rewrite; work stayed milestone-sized and reversible.
- **M19** authorized and **delivered** **only** the **Candidate A** Python-first audio seam (see **`docs/acoustic_kernel_surface_inventory.md`** §5 and **`DEVELOPMENT.md`**) — **not** calculator extraction, TFLite decoupling, or **`@com_google_audio_tools`** integration in **`aurora/`**. **M20** (**delivered** as **PR #21**) adds **Candidate B**-style **evidence-backed graph mapping** (see **`docs/audio_classifier_graph_mapping.md`**) — **not** calculator extraction or correctness claims by itself. **M21** (**delivered** as **PR #22**) adds **bounded** native ctypes wiring for **`AUDIO_FROM_FILE`** (**`audio_native_bindings`**, **`NativeAudioDispatcher`**) — **not** decode correctness, graph execution proof, or Tasks parity. **M22** (**delivered** as **PR #23**) adds the same **structural** **`MpAudioClassifier*`** path for **`AUDIO_FROM_BYTES`** — **not** decode correctness, graph/TFLite correctness, or MediaPipe parity; **local-native** proof was **not attempted** (no suitable pre-built Tasks shared library in scope; building one is **out of M22 scope** — see workspace **`docs/milestones/M22/M22_local_native_proof.md`**).
- **Did not prove** full kernel implementation, **task-base** extraction (`VisionTaskBase` / `AudioTaskBase`), **native** or **MediaPipe** application correctness, or **upstream** Tasks migration — CI and governance remained **structural** proof for the first-party seam.
- **Competition rail** remains **parallel** and **protected** — a credible BirdCLEF submission path must stay **possible** at the planning level; baseline layout and harness stay **deferred** unless a later milestone locks them.

### Phase D closure criteria (recorded at closeout)

Phase D is **closed** under the **Interpretation B** framework from workspace **`docs/milestones/M22/phase_d_closeout_readiness_audit.md`**, including the **honest fallback** when **local-native** proof is **not attempted**: **structural** completion + **external** / out-of-scope blocker documentation — **without** implying that a real native library was exercised.

**Closure criteria (all satisfied for M23):**

1. **Kernel ingress surfaces** are documented in committed repo docs: **`docs/kernel_ingress_strategy.md`**, **`docs/acoustic_kernel_surface_inventory.md`**, **`docs/audio_classifier_graph_mapping.md`**.
2. **Structural** Python + **ctypes** seams for audio dispatch exist for both **`AUDIO_FROM_FILE`** and **`AUDIO_FROM_BYTES`** on **`NativeAudioDispatcher`** (**M21**, **M22**), with fake-backed tests and **`ci` / `repo-safety`** structural proof — **not** decode, graph, or application correctness.
3. **CI** remains **truthful** for the declared **structural** bar (**`ci` / `repo-safety`** — unchanged identity); green **does not** claim native execution, decode, or graph correctness.
4. **Local-native** proof: **not attempted** — no suitable pre-built Tasks **AudioClassifier** shared library in scope for a bounded attempt; building one is **out of scope** for **`aurora/`** and the read-only **`mediapipe/`** workspace policy (**M22** record). This is classified as an **external / tooling absence** and **scope boundary**, **not** a defect in the first-party seam code path.
5. **Explicit non-proofs** are recorded in this file: **no** decode correctness; **no** graph/TFLite correctness; **no** MediaPipe application parity; **no** CI-native real-library execution; **no** claim that **local-native** proof succeeded.

Phase D does **not** require **CI-native** exercise of a real MediaPipe library. Decode, graph, parity, and native-in-CI proof remain **deferred** to later milestones unless explicitly authorized.

### Phase D closure matrix (M17–M22)

| Milestone | What Phase D **proved** (bounded) | What Phase D **did not prove** |
|-----------|-----------------------------------|--------------------------------|
| **M17** | **Docs-only** ingress framing — **`docs/kernel_ingress_strategy.md`**; Phase D entry constraints in **`docs/aurora.md`**. | Runtime behavior; native/graph correctness. |
| **M18** | **Docs-only** surface inventory — **`docs/acoustic_kernel_surface_inventory.md`**; M19 slice recommendation. | Kernel implementation; **`@com_google_audio_tools`** integration in **`aurora/`**. |
| **M19** | **Structural** Python seam — **`AuroraAudio`**, **`audio_dispatch.py`**, **`AUDIO_*`** tokens; fake-backed tests; **`ci` / `repo-safety`**. | Decode; MediaPipe parity; real native library. |
| **M20** | **Docs-only** upstream **AudioClassifier** graph mapping — **`docs/audio_classifier_graph_mapping.md`**; BUILD vs **`audio_classifier_graph.cc`** framer nuance (**no** spurious dedicated framer **node** in **`audio_classifier_graph.cc`**). | Runtime proof; D1 wiring. |
| **M21** | **Structural** **`AUDIO_FROM_FILE`** — **`audio_native_bindings`**, **`NativeAudioDispatcher`**; fake-backed ctypes shape. | Decode; graph execution; Tasks parity; real library on CI. |
| **M22** | **Structural** **`AUDIO_FROM_BYTES`** — same lifecycle as file path; **`AudioNativeBytesDeferredError`** removed; workspace note on **local-native** non-attempt. | Decode (bytes → samples); graph/TFLite; parity; **local-native** run (**not attempted**). |
| **M23** | Phase D **governance** closure — **PR #24** **merged** to **`main`** — matrix + criteria in committed record. | Anything listed as explicit non-proof above. |

**What Phase D still did not prove at closeout:** end-to-end **working** acoustic kernel in the sense of verified decode + graph execution + application-correct outputs; **VisionTaskBase** / **AudioTaskBase**; ARB **implementation** (M25 defines **boundary/spec** only — see **`docs/aurora_run_bundle_boundary.md`**); ORNITHOS; BirdCLEF harness; real **Tasks** shared library exercise (**local** or **CI**).

---

## Program Roadmap (proposed)

This section is a **planning map** — a proposal aligned to the vision and PANTANAL workstreams, **not** a contract. Milestone IDs and ranges may be adjusted; **Execution Phase Boundaries (locked)** still govern what kind of work is allowed when.

### Long-horizon program map (M01–M33)

| Program phase | Theme | Milestones (proposed) |
|---------------|--------|------------------------|
| **I** | Governance closure & safe-to-change baseline | M01–M05 |
| **II** | Runtime seam normalization | M06–M09 |
| **III** | Surface coherence & task abstraction | M10–M14 |
| **IV** | Acoustic kernel extraction | M15–M19 |
| **V** | Artifact runtime & certification boundary | M20–M23 |
| **VI** | ORNITHOS model stack | M24–M28 |
| **VII** | PANTANAL-1 submission, observatory, case study | M29–M33 |

**Roadmap note:** The **M20**–**M23** band in the table is a **proposed** long-horizon theme only. **M20** is expected to function as a **transition / scope-lock** milestone between bounded Phase D ingress work and later artifact-runtime work; **Execution Phase Boundaries (locked)** and the **milestone ledger** remain authoritative over this proposed map.

**M20–M23 band note:** The proposed **Phase V** label in the long-horizon table does **not** override actual execution. **M20**–**M22** were delivered as **Phase D** milestones (**bounded** ingress / mapping / structural D1); **M23** is **Phase D** governance closeout. The **Artifact runtime & certification boundary** theme shifts to **M25+** (tentative **workspace** planning only — **no** new formal post-Phase-D execution-phase label canonized in this file until a later milestone authorizes one). **M24** **governance** records downstream ecosystem roles and **dependency directionality** (see **Proposed downstream repo layout and dependency directionality**); **M24** is **not** ORNITHOS implementation. **Execution Phase Boundaries (locked)** and the **milestone ledger** remain authoritative.

**Program phase VI note:** The long-horizon table labels **M24–M28** as **ORNITHOS model stack**. **M24** in this repository is **docs/governance only** (Phase E readiness audit + ecosystem boundary charter). **ORNITHOS** implementation work **does not** begin by default in **M24**. **Execution Phase Boundaries (locked)** and the **milestone ledger** remain authoritative over this proposed map.

**Program phase IV note (read with ledger, not instead of it):** The **M15–M19** range is **approximate**; **locked execution** is **not** defined by this table. **M15** was the last **substantive Phase C** code milestone; **M16** was **Phase C** governance closeout only; **actual Phase D entry** (planning / ingress / scoping) begins at **M17**. Phase **C** closure was justified by the **tree and milestone ledger**, not by program-phase numbering.

### Protected competition rail (C01–C05) — proposal only

A separate **C01–C05** track is reserved for **baseline and submission safety** (credible BirdCLEF path, frozen candidates, compliance). It runs **alongside** refactor phases; integration points with AURORA runtime surfaces are **not** fixed in M01. Location, tooling, and repo structure for this rail remain **deferred**.

### Next milestone (M26)

**M26** — **stub only** (workspace **`docs/milestones/M26/`**); **no** formal scope pre-authorized until **M26** plan is locked.

### Milestone M25 (ARB boundary framing and minimal run-bundle specification) — **complete**

**M25** — **docs/governance only** — **`main`**: **PR** **#26** — https://github.com/m-cahill/aurora/pull/26 — **`gh pr merge 26 --merge --admin`**. **Merge commit:** **`4037db3b0c037efea533e1a353565c9f70139a14`** (**2026-04-02T02:57:26Z** UTC). **Delivered:** **`docs/aurora_run_bundle_boundary.md`** (new); **`docs/aurora.md`**; **`scripts/verify_repo_state.py`** (requires **`aurora_run_bundle_boundary.md`** + canonical reference); **`tests/test_verify_repo_state.py`**; **`DEVELOPMENT.md`**. **Final pre-merge PR head:** **`6eeac778651e5cd7c92d4d97f526ee8cbca186a8`**. **Proof:** **`ci` / `repo-safety`** on **PR** **head** **`23881492177`** (`push`, **success**), **`23881499188`** (`pull_request`, **success**); post-merge **`main`** **`23881508861`** (**success**, head **`4037db3b0c037efea533e1a353565c9f70139a14`**) — **not** new runtime proof. **Explicit** — **M25** did **not** canonize a new **formal post–Phase D execution-phase label** in **Execution Phase Boundaries (locked)** (see **`docs/aurora_run_bundle_boundary.md`** §8). **Grounding:** **`AURORA_VISION.md`** §9.2 suggested bundle shape (workspace — **not** committed here). **Normative determinism** in **M25:** **offline/batch replay** only; **LIVE_STREAM** callback order **not** the contract; training-run recording **deferred** / **open**. **Certification:** RediAI **external**. **Not in M25:** `src/aurora/`; ARB writer/reader/schema/hash; decode/graph/native proof; ORNITHOS/PANTANAL-1 repos; competition implementation. **Does not prove:** ARB **implementation**; runtime **behavior** change; decode, graph, parity, native execution, competition readiness. **M25** **closeout project record on `main`:** **`12fac02dcd9e88fea72e9c5e3da11c3da585b12b`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23881539057`** (**success**). **Authoritative final enumerated `main` tip (M25 closeout project record):** **`12fac02dcd9e88fea72e9c5e3da11c3da585b12b`**. **Closeout recorded (UTC):** **2026-04-02T02:58:35Z**. **Authoritative `main` tip immediately after merge (merge commit):** **`4037db3b0c037efea533e1a353565c9f70139a14`**.

### Milestone M24 (Phase E readiness audit + ecosystem boundary charter) — **complete**

**M24** — **Phase E readiness audit** and **ecosystem boundary charter** (**docs/governance** **only**) **on** **`main`**: **PR** **#25** — https://github.com/m-cahill/aurora/pull/25 — **`gh pr merge 25 --merge --admin`**. **Merge commit:** **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`** (**2026-04-02T01:41:38Z** UTC). **`docs/aurora.md`** — **Proposed downstream repo layout and dependency directionality** (proposed **`ornithos/`** / **`pantanal-1/`** siblings — **not** created by **M24**); **no** formal **post–Phase D** execution-phase label canonized in **Execution Phase Boundaries (locked)** by default. **Workspace:** **`docs/milestones/M24/phase_e_readiness_audit.md`**. **Not in M24:** `src/aurora/` runtime changes; `mediapipe/` edits; ORNITHOS/PANTANAL-1 repo creation; Kaggle/submission/training implementation. **Final pre-merge PR head:** **`f9d9fbe3603d10402023b42896efb9d96e10e4d0`**. **Proof:** **`ci` / `repo-safety`** on **PR** **head** **`23879281514`** (`push`, **success**), **`23879282695`** (`pull_request`, **success**); post-merge **`main`** **`23879539276`** (**success**) — **not** new runtime proof. **Does not prove:** decode, graph, parity, native execution, competition readiness, or that downstream repos exist. **Authoritative `main` tip immediately after merge (merge commit):** **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`**. **Closeout project record on `main`:** **`21985bf488b67ccf4c62ba2fcaeac16c1d6429cd`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23879695944`** (**success**). **Authoritative final enumerated `main` tip (M24 closeout project record):** **`21985bf488b67ccf4c62ba2fcaeac16c1d6429cd`**.

### Milestone M23 (Phase D governance closeout) — **complete**

**M23** — **Phase D governance closeout** (**docs-only**) **on** **`main`**: **PR #24** — https://github.com/m-cahill/aurora/pull/24 — **`gh pr merge 24 --merge --admin`**. **Merge commit:** **`64cc610d690e61edb026c37ec7cd1915a1fbba89`** (**2026-04-02T00:23:40Z** UTC). **`docs/aurora.md`** — Phase D **closed**; **Phase D closure criteria** + **closure matrix** (**M17**–**M22**); explicit **proved / not proved** language including **local-native** proof **not attempted** (**external** / out-of-scope — workspace **`docs/milestones/M22/M22_local_native_proof.md`**); **`docs/kernel_ingress_strategy.md`** §11; **`docs/runtime_seam_framing.md`** status pointer; **no** `src/aurora/` runtime changes; **no** C++/BUILD/Bazel/TFLite work in **`aurora/`**; **no** `mediapipe/` modification. **Proof:** **`ci` / `repo-safety`** on **PR** **head** **`23876960921`** (`push`, **success**), **`23876961935`** (`pull_request`, **success**); post-merge **`main`** **`23877456336`** (**success**) — **not** new runtime proof. **Does not prove:** decode, graph, parity, or native execution. **Final pre-merge PR head:** **`8de685a729d4bdd9d1d131fc9b5c683809e84929`**. **Closeout project record on `main`:** **`9d8296d449c42a43a505f539a0736a4b0a213de4`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23877506186`** (**success**). **Authoritative final enumerated `main` tip (M23 closeout project record):** **`9d8296d449c42a43a505f539a0736a4b0a213de4`**.

### Milestone M22 (bounded D1 — native **`AUDIO_FROM_BYTES`**) — **complete**

**M22** delivered **`NativeAudioDispatcher.dispatch(AUDIO_FROM_BYTES, …)`** on **`main`** (**PR #23**): same **create → classify → close-result → close** lifecycle as **`AUDIO_FROM_FILE`**; **`MpAudioData`** from **`bytes`** via the same **structural** float32-lane mapping as the file-backed path (**not** codec decode); **`AudioNativeBytesDeferredError`** removed. **`audio_dispatch.py`** unchanged (frozen **`dispatch(token, payload, lib)`**). **Proof:** structural **`ci` / `repo-safety`** on fake-backed tests — **not** real MediaPipe library exercise in Actions. **Does not prove:** decode correctness, graph/TFLite correctness, or MediaPipe application parity. **Local-native** proof: **not attempted** — no suitable pre-built Tasks **AudioClassifier** shared library available in scope; building one **out of M22 scope** (honest non-claim; workspace **`docs/milestones/M22/M22_local_native_proof.md`**). **Merge commit:** **`19859006a13eafc7978694ba5e14a61829573f17`** — ledger row **M22**. **M20** framer guardrail unchanged: **BUILD** vs **`audio_classifier_graph.cc`** nuance remains authoritative — **no** implication of a dedicated **`time_series_framer_calculator`** **node** in **`audio_classifier_graph.cc`**.

### Milestone M21 (bounded D1 — native **`AUDIO_FROM_FILE`**) — **complete**

**M21** delivered the first **D1** slice on **`main`** (**PR #22**): **`src/aurora/runtime/audio_native_bindings.py`** (ctypes **`MpAudioClassifier*`** surface) and **`src/aurora/runtime/native_audio_dispatcher.py`** (**`NativeAudioDispatcher`**) so **`AUDIO_FROM_FILE`** can route through the Tasks C API **when** a suitable shared library is supplied via **`LibraryLoader`**. **`audio_dispatch.py`** remains the seam-level caller. **Proof:** structural **`ci` / `repo-safety`** (symbol wiring, call shape, error translation). **`AUDIO_FROM_BYTES`** on **`NativeAudioDispatcher`** completed in **M22** (**PR #23**). **Does not prove:** decode correctness, graph/TFLite correctness, or MediaPipe application parity. **Merge commit:** **`12c3e91bd097c0e4a9c9a1ca00862bf9a6d9badf`** — ledger row **M21**.

### Milestone M20 (scope lock — docs) — **complete**

**M20** — **audio classifier graph mapping and native-wiring target lock** (**docs-only**). **Purpose:** Document the upstream **Python → C API → C++ `AudioClassifier` → `AudioClassifierGraph`** chain grounded in the workspace **`mediapipe/`** clone (paths under **`mediapipe/mediapipe/`** from workspace root), centered on **`tasks/python/audio/audio_classifier.py`**, **`tasks/c/audio/audio_classifier/`**, **`tasks/cc/audio/audio_classifier/audio_classifier_graph.cc`**, **`AudioToTensorCalculator`**, and inference / post-processing — and lock **proof obligations** vs **non-goals** for any future milestone that would wire the first-party audio seam to a real path. **M20** is **planning / documentation / scope-lock first**: **no** default **`src/aurora/`** runtime behavior change, **no** C++/BUILD/TFLite integration in **`aurora/`**, **no** `mediapipe/` copy, **no** claim of MediaPipe/native/decode correctness. **BUILD** lists **`time_series_framer_calculator`** for the **`audio_classifier_graph`** target; the inspected **`audio_classifier_graph.cc`** composes **`AudioToTensorCalculator`** directly (framing inside that calculator) — see **`docs/audio_classifier_graph_mapping.md`** §3.2. **Status:** **complete** on **`main`** — **PR #21** — https://github.com/m-cahill/aurora/pull/21 — merge commit **`c7e9de3da82e81ac697f73ddde55708151f6529c`** (ledger row **M20**).

---

## Success criteria (repo-level)

- **Governance success** — the repo can be changed safely; CI is truthful when present; dependencies and boundaries are explicit; refactors are classifiable and revertible.
- **Runtime success** — loader/dispatcher paths are explicit and testable; duplicated runtime behavior moves behind stable abstractions where justified; audio flows read as a coherent subsystem.
- **Kernel success** — ingestion → features → model → outputs forms a governed pipeline with explicit artifacts and credible offline replay; async semantics stay honest.
- **Case-study success** — Foundry methodology is demonstrated on a real system with publishable architectural evidence; BirdCLEF and PANTANAL-1 can use the story without conflating leaderboard rank with architectural proof.

---

## Working rules for future milestones

1. **Classify** each change (governance, seam, API, kernel, model, artifact boundary, observatory) before coding.
2. **Preserve behavior** unless the milestone explicitly intends otherwise and proves it.
3. **Prefer seam extraction and small moves** over broad rewrites.
4. **Keep milestones small and reversible**, with explicit acceptance criteria and recorded commit SHAs.
5. **Use evidence over narration** — tests, audits, artifacts, diffs, CI logs.

---

## Deferred topics / not yet designed

- ARB **implementation** (writer/reader/schema/hash, tooling, canonical bundle format) — **M25** committed **`docs/aurora_run_bundle_boundary.md`** (**boundary/spec only**; vision §9.2 **grounding** — **not** runtime code)
- ORNITHOS architecture and training/inference split
- Certification integration (e.g., RediAI) and CI gating details
- Competition baseline **location, repo layout, and harness** (C01–C05 content)
- Plugin/extension system and task discovery
- Legacy Solutions deprecation timeline and migration guarantees
- **Upstream Tasks `image.py` / fork wiring** — **M08**–**M09** delivered **first-party** bounded **`AuroraImage`** and **M09** composed **smoke** coverage in-repo (not upstream migration); upstream migration remains **deferred** and was **not** required for **Phase B** closure in the tracked repo (**M10**); see `docs/runtime_seam_framing.md`, `docs/runtime_surface_strategy.md` §6a, and workspace `docs/phase_summaries/phase_b_summary.md`

---

## Milestone ledger

| ID | Title | Status | Initialized | Purpose | Closeout summary |
|----|-------|--------|-------------|---------|------------------|
| **M01** | Governance bootstrap, canonical docs initialization, and README anchor | **Complete** | 2026-03-25 | Establish `aurora/docs/aurora.md` as the committed source of truth; minimal `README.md`; separate locked phases from proposed roadmap; no runtime or `mediapipe/` changes. | Delivered substantive `docs/aurora.md` and minimal `README.md` on PR #1 (`m01-governance-bootstrap` → `main`). Scope verified docs-only: no `mediapipe/` edits; no runtime/code under `aurora/`. Primary delivery commit `f32c205ae21010ed3f09e841d7debc16a65c5095`. Closeout commit `742d9f0ff9c4a9e055d5fd28f46f1c8d265ccc8e` (ledger + header). **PR #1** merged to `main` (merge commit `2f4efea`). **Closeout recorded (UTC):** 2026-03-26T04:00:00Z. |
| **M02** | Truthful CI skeleton and repo safety rails | **Complete** | 2026-03-25 | First GitHub Actions workflow (`ci` / `repo-safety`), stdlib repo verifier, Ruff on `scripts/`, hygiene files; no runtime seam work. | **PR #2** merged to `main` (**merge commit `4e3843c9366b745363898fa2f5a273901ed73a8d`**, merged **2026-03-26T06:10:00Z**). Branch `m02-truthful-ci-skeleton` @ **`d9c67114a931809b178d855db16c10171482acd7`**; primary implementation **`4d1817eb4ecdf18b311babb195a479965682202b`**. Workflow **`.github/workflows/ci.yml`** (`ci`); job **`repo-safety`**; intended check name **`ci / repo-safety`**. Latest green runs on PR head: **`23577639206`** (pull_request, **success**), **`23577638447`** (push, **success**). Post-merge **`main`**: successive pushes triggered **`ci` / `repo-safety`** — **all success** (representative run IDs: **`23580213498`**, **`23580245540`**, **`23580265101`**, **`23580299720`**; later doc-only commits may add further runs — see workspace **`M02_summary.md`**). Artifact: **`m02-ci-artifacts`**. **Branch protection / required checks not configured** in this session (manual follow-up). Evidence: workspace `M02_summary.md`, `M02_run1.md`, `M02_audit.md`. |
| **M03** | Branch protection enforcement and CI governance hardening | **Complete** | 2026-03-26 | Harden verifier (full SHA pins for external Actions, `ci` / `repo-safety` identity), stdlib tests under `tests/`, CI artifact **`ci-artifacts`**, `DEVELOPMENT.md`; merge discipline on `main` via branch protection when permitted. | **PR #3** merged to `main` (**merge commit `ce53b7c0a2d596ed3adf05916a68551cf30104ff`**, **2026-03-26**). Branched from **`3d3830e07635ea97878bb714ccd399470fceae61`** (post-M02 `main`); primary implementation **`8716ab5509d606e087c50139bc268f273264d5bc`**. Workflow **`ci`**; job **`repo-safety`**; required check **`ci / repo-safety`**. Representative green runs: **`23621970097`** (push, feature branch), **`23621984590`** (pull_request), **`23622016833`** (push, post-merge `main`). **Branch protection on `main`:** **enabled** — `required_status_checks.strict: true`, required context **`ci / repo-safety`** (GitHub Actions app_id **15368**), **`required_pull_request_reviews`** with **0** approvals (PRs required). Evidence: `gh api repos/m-cahill/aurora/branches/main/protection`; workspace **`M03_summary.md`**. |
| **M04** | Tracked runtime surface strategy and Phase B entry gate | **Complete** | 2026-03-26 | Governance only: **`docs/runtime_surface_strategy.md`**, **`docs/aurora.md`**, verifier + tests, **`DEVELOPMENT.md`**; **Phase A continues**; **Phase B** only after **M05** closes. | **PR #5** merged to `main` (**merge commit `e5125d93f96d9178feee2859b6b474090aa22721`**, merged **2026-03-27**). Implementation **`4702e9c2757c04582afb6c1e081c2c46b7b104a5`**; baseline **`13e0cded5a65d92dd0c2d75dd7b496fe36f8b6f2`**. Workflow **`ci`** / job **`repo-safety`** — authoritative PR run **`23625478660`** (**pull_request**, **success**). Post-merge `main` push run **`23625501522`** (**success**, merge commit **`e5125d9`**). Closeout commit **`bcead1349d262dd8b02af5df25f199badd9ed054`**; closeout `main` run **`23625512581`** (**success**). **No** `mediapipe/` edits in `aurora/`; **no** runtime seam code. **Closeout recorded (UTC):** 2026-03-27T00:55:00Z. |
| **M05** | Provenance-preserving runtime substrate establishment and Phase A closeout gate | **Complete** | 2026-03-26 | First-party package **`src/aurora/`**, **`docs/runtime_substrate.md`**, substrate tests, CI (`PYTHONPATH=src`), verifier gates; **Phase A closed**; **Phase B authorized**; **M06** seeded. | **PR #6** → `main` (**merge `04a5527c43c6e5cf64a0ec6beda45181f1c83efc`**, merged **2026-03-27**); implementation **`107b49d476d9854df58527aa0b56eb6e134e88d3`**; baseline **`bbbbb9c246b76beb943c1d56168e8d0c503f8d77`**. **`ci` / `repo-safety`:** PR **`pull_request`** `23627396629`, **`push`** `23627394261`; post-merge **`main`** `23627411903`; closeout doc commit on **`main`** `23627439447` — **all success**. **No** `mediapipe/` imports under `src/aurora/`; **no** dispatcher / `image.py` / `LibraryLoader` implementation. **Closeout recorded (UTC):** 2026-03-27T01:59:27Z. |
| **M06** | Dispatcher boundary formalization and runtime seam framing | **Complete** | 2026-03-26 | First **Phase B** slice: minimal **`Dispatcher`** / **`LibraryLoader`** `Protocol`s under **`src/aurora/runtime/`**, **`docs/runtime_seam_framing.md`**, verifier + tests, **`DEVELOPMENT.md`**; honest LIVE_STREAM / async wording; **not** `image.py` migration, task bases, kernel, or MediaPipe/native correctness. | **PR #7** → `main` (**merge `0862c5bf96baa681d29a5283e0ca949588a86565`**, merged **2026-03-27**); implementation **`9eb2711a5d4de6b3d597a7d25b2c8105f56bf508`**; baseline **`ed22eaf590d565da4619a1ee7a6f0d2632b978e6`**. **`ci` / `repo-safety`:** PR **`pull_request`** `23630086059`, **`push`** `23630082135`; post-merge **`main`** `23631004105` (merge commit); closeout project record on **`main`** **`17e8069cbfe67511c05e9bc216701f38096a2316`** — **`main`** run **`23631039823`** — **success**; ledger update on **`main`** **`0a4c6c89dbc8f2826d17127f7ca5f472aa1eae15`** — **`main`** run **`23631054922`** — **success**; final project record on **`main`** **`61b71a62307481feaf41d336c5a695e2d305778c`** — **`main`** run **`23631078586`** — **success**; authoritative `main` tip after closeout doc sequence **`fcec0d81923409b5ea57a73d17eb0fbe0503d317`** — **`main`** run **`23631103294`** — **success**; final ledger on **`main`** **`d5b0d78a8ff286e4264477b517226e24e0cce1cd`** — **`main`** run **`23631124779`** — **success**. **Proves:** contract-level seam formalization + governance tripwires. **Does not prove:** MediaPipe behavior, native dispatch/graph correctness, `image.py` migration, concrete loader wiring. **Closeout recorded (UTC):** 2026-03-27T04:20:55Z (merge time). |
| **M07** | Concrete shared-library loader implementation and image-migration readiness | **Complete** | 2026-03-27 | Second **Phase B** slice: **`SharedLibraryLoader`** / **`SharedLibraryLoadError`** (`ctypes.CDLL`, explicit path, per-instance memoization), **`docs/runtime_seam_framing.md`** + **`DEVELOPMENT.md`**, verifier tracks **`shared_library_loader.py`**, **`tests/test_library_loader_runtime.py`** (patched `CDLL`); **not** `image.py` migration, domain smoke tests, task bases, kernel, or MediaPipe/native correctness. | **PR #8** → `main` (**merge `ca31fa7af428b1335af4b34f45bca639e0cec731`**, merged **2026-03-27T20:50:35Z**; **merge commit** method; **`--admin`** required). **PR head:** **`bc0bb4d3458fe566d93cfc530547c6a3b93dd687`**; **primary implementation:** **`aca63bab931f7b21d8cbf3f2fca20c4df1d2aa2d`**. **`ci` / `repo-safety`:** PR **`push`** `23664282232`, **`pull_request`** `23665065804`; post-merge **`main`** `23666946080` (**success**, head **`ca31fa7`**). **Proves:** concrete first-party **`LibraryLoader`** implementation + memoized load/failure + governance tripwires; tests use patched **`CDLL`** only. **Does not prove:** MediaPipe/native behavior, real host library paths, **`image.py`** migration, domain smoke, task bases, kernel. **Closeout recorded (UTC):** 2026-03-27T20:50:35Z (merge time). **Closeout doc commit on `main`:** **`3bb5ac24f035404843139dd7cfeae0db16de9bfb`**; post-closeout **`main`** **`ci`** run **`23667007934`** (**success**). **Ledger update commit:** **`f74c7b9ded6dfd9839331011e6016bff7d5f24d7`**; **`main`** run **`23667048907`** (**success**). **Ledger enumeration commit:** **`e400e84dc7693fa2a201372f8176da3914b3fafa`**; **`main`** run **`23667092136`** (**success**). **Last enumerated `main` tip:** **`e400e84dc7693fa2a201372f8176da3914b3fafa`**. |
| **M08** | Bounded `image.py` migration onto Dispatcher and loader seams (first-party `AuroraImage`) | **Complete** | 2026-03-27 | Third **Phase B** slice: **`src/aurora/runtime/image.py`** — **`AuroraImage`**, **`ImageCreationError`**, route through **`Dispatcher.dispatch`** + **`LibraryLoader.shared_library`**, no **`ctypes`** import in **`image.py`**, **`tests/test_image_runtime.py`**, verifier + **`docs/runtime_seam_framing.md`** + **`DEVELOPMENT.md`**; **not** upstream Tasks **`image.py`**, domain smoke, task bases, kernel, MediaPipe parity. | **PR #9** → `main` (**merge `ba0110dd8f9b41e60213d026c50dc6a893c1c8f6`**, merged **2026-03-27T23:27:38Z**; **`gh pr merge 9 --merge --admin`**). **PR head:** **`6c59f3ea7cdcae33981a10af0d327a5094d4ed01`**; **primary implementation:** **`90ea5722187993942408bcca15405a422875a8e6`**. **`ci` / `repo-safety`:** PR head **`23669305536`** (`push`), **`23670441763`** (`pull_request`); post-merge **`main`** merge push **`23671790446`** (**success**, head **`ba0110d`**); closeout project record on **`main`** **`dc44fd72d98ca27ac9de6648f0ab83aa3094527f`** — **`main`** run **`23671809738`** (**success**); ledger update on **`main`** **`55f435be901d0a0e06b40f3cb929c2b5bd5dfd60`** — **`main`** run **`23671822595`** (**success**). **Proves:** bounded first-party **`AuroraImage`** seam; routing via injected **`Dispatcher`** / **`LibraryLoader`**; no direct native load in **`image.py`**; **`ImageCreationError`** chaining; fake-backed tests; verifier structural governance. **Does not prove:** MediaPipe/native correctness or parity; decode correctness; real native execution on CI; upstream **`image.py`** migration; domain smoke; task bases; kernel. **Closeout recorded (UTC):** 2026-03-27T23:27:38Z (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`ba0110dd8f9b41e60213d026c50dc6a893c1c8f6`**. **Last enumerated `main` tip in this record:** **`55f435be901d0a0e06b40f3cb929c2b5bd5dfd60`**. |
| **M09** | Bounded domain smoke tests for current first-party runtime surfaces | **Complete** | 2026-03-27 | Fourth **Phase B** slice: **`tests/test_runtime_smoke.py`** — composed **`AuroraImage`** + real **`SharedLibraryLoader`** + patched **`ctypes.CDLL`**, recording fake **`Dispatcher`**; **`DEVELOPMENT.md`**, **`docs/runtime_seam_framing.md`** §1c/§6c; **not** upstream Tasks **`image.py`**, verifier change, real native execution, task bases, kernel, MediaPipe parity. | **PR #10** → `main` (**merge `a7944adb20f4137ea3999b460daeefdb2290a06f`**, merged **2026-03-28T00:58:51Z**; **`gh pr merge 10 --merge --admin`**). **PR head:** **`373996d370d4d1a1c5482425b5b15c5e514efdae`** (implementation **`373996d`**). **`ci` / `repo-safety`:** PR head **`23673335182`** (`push`), **`23673338673`** (`pull_request`). Post-merge **`main`** merge push **`23673835497`** (**success**, head **`a7944ad`**). Closeout project record on **`main`** **`3af2205ad13f042c88fe6ef932893e4594a8b39f`** — **`main`** **`23673854790`** (**success**); ledger enumeration **`cde5f1a3296db5ed075e3718651e789bea135d4c`** — **`main`** **`23673871240`** (**success**); **`9077726492d80731218336b42b2ec29b86c2498b`** — **`main`** **`23673882954`** (**success**); project record **`3e3455079516bfbe7d374408c6dc0691ba1ccf7c`** — **`main`** **`23673909636`** (**success**); ledger closeout **`05ee473182bdb820775d1ca3865caf60e4af1c88`** — **`main`** **`23673928887`** (**success**); final enumeration **`573d4baa32da9c097a2f960509e602c4539eeff9`** — **`main`** **`23673941506`** (**success**). **Proves:** bounded **composed** smoke coverage for the **current** first-party runtime seam; **`AuroraImage`** through **`SharedLibraryLoader`** (patched **`CDLL`**) and fake **`Dispatcher`**; happy and failure paths at composed seam level. **Does not prove:** MediaPipe/native correctness or parity; decode correctness; real native execution on CI; upstream **`image.py`** migration; placeholder multi-domain smoke; task bases; kernel. **Closeout recorded (UTC):** 2026-03-28T00:58:51Z (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`a7944adb20f4137ea3999b460daeefdb2290a06f`**. **Last enumerated `main` tip in this record:** **`573d4baa32da9c097a2f960509e602c4539eeff9`**. |
| **M10** | Phase B closeout and Phase C authorization gate | **Complete** | 2026-03-27 | **Governance only:** mark **Phase B** **closed** in **`docs/aurora.md`**; **`docs/runtime_surface_strategy.md`** §6a; workspace **`docs/phase_summaries/phase_b_summary.md`**; **Phase B** closure matrix (**M06**–**M09**); **upstream Tasks `image.py` / fork wiring** **deferred**, **non-blocking**; **authorize M11** / **Phase C**; **no** `src/aurora/` implementation. | **PR #11** → `main` (**merge `7670b0b684727d36c692bee50aaa30fd672e56db`**, merged **2026-03-28T03:43:25Z**; **`gh pr merge 11 --merge --admin`**). **PR head:** **`49b6eec157bbe941a2d358055feb3966742ea435`** (implementation commits **`49b6eec`**, **`aea3200`**). **`ci` / `repo-safety`:** PR head **`23676589956`** (`push`), **`23676592178`** (`pull_request`) — **success**. Post-merge **`main`** merge push **`23676742192`** (**success**, head **`7670b0b`**). Closeout project record on **`main`** **`e7d4276e8d00e2f7f848a0c7efc3e6093f3d8cae`** — **`main`** **`23676752278`** (**success**); ledger enumeration **`74243c45e5a12d2d5e3fd7e0a1932ae26c54f534`** — **`main`** **`23676760936`** (**success**); project record **`460cb5fc1bc31b757175446d555c0bf0d5fb64c5`** — **`main`** **`23676770488`** (**success**); final ledger **`0de923b7ad5037b4eed28bda1bf156d9a9b13f73`** — **`main`** **`23676778113`** (**success**); enumerate complete **`38f572b7b84b89de52f30c3ba10b99d9a23e4597`** — **`main`** **`23676787018`** (**success**); project record **`51e2cdfed3947521c191262fcec0fab1aca5466c`** — **`main`** **`23676795401`** (**success**). **Proves:** tracked **Phase B** seam contract satisfied per ledger (**M06**–**M09**); **Phase B** **closed**; **Phase C** **authorized**. **Does not prove:** upstream **`image.py`** migration; MediaPipe/native parity; **Phase C** code. **Baseline:** **`110e27df59e0cd9f464251051a5d6c9268a49643`**. **Closeout recorded (UTC):** **2026-03-28T03:43:25Z** (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`7670b0b684727d36c692bee50aaa30fd672e56db`**. **Last fully enumerated `main` tip in this record:** **`51e2cdfed3947521c191262fcec0fab1aca5466c`**. |
| **M11** | Phase C kickoff: honest coverage baseline and first enforcement gate | **Complete** | 2026-03-27 | First substantive **Phase C** slice: **`coverage`** + **`.coveragerc`** (**`src/aurora/`** only, **`fail_under=100`** line coverage), **`ci` / `repo-safety`** **`coverage run` / `report` / `json`**; **`Dispatcher` / `LibraryLoader`** Protocol stub lines **`pragma: no cover`**; **`DEVELOPMENT.md`**; **no** behavior change to runtime contracts beyond pragma; **no** `pytest`; **no** `mediapipe/` work. | **PR #12** → `main` (**merge `0ea5e37f08c549c7a9389fd1f5a967a41a3776e3`**, merged **2026-03-28T04:57:07Z** UTC; **`gh pr merge 12 --merge --admin`**). **PR head (pre-merge):** **`e0b19c6d4870d2f66bd8c740edb52f84e60e4784`**. **Primary implementation:** **`7c72cd4975ea969a2fbb4a6a3af38bb884489fad`**. **`ci` / `repo-safety`:** PR head **`23677838389`**, **`23677851680`**, **`23677852220`** — **success**. Post-merge **`main`** merge push **`23677929979`** — **success** (head **`0ea5e37f08c549c7a9389fd1f5a967a41a3776e3`**). **Closeout project record on `main`:** **`284ce3cec1412e4d1ee007e1483a53b967ad0852`** — **`main`** **`23677944622`** (**success**); final project record **`ea64ff47d470cef3c9f75ce3e831b330afa70700`** — **`main`** **`23677956122`** (**success**). **Measured baseline (pre-pragma Protocol stubs):** **98%** line coverage on **`src/aurora/`** (**96** statements, **2** miss: structural **`...`**); **post-pragma report:** **100%** (**94** statements), **`fail_under=100`**. **Proves:** first-party **`src/aurora/`** line coverage measured and gated on **`main`**. **Does not prove:** branch coverage exhaustiveness; governance scripts under **`scripts/`**; MediaPipe/native behavior. **Closeout recorded (UTC):** **2026-03-28T04:57:07Z** (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`0ea5e37f08c549c7a9389fd1f5a967a41a3776e3`**. **Last fully enumerated `main` tip (M11 closeout):** **`ea64ff47d470cef3c9f75ce3e831b330afa70700`** (**`main`** **`23677956122`**). **Post-closeout record alignment:** **`02bb04e26105c45a56f5e49f5f01bae00a7d746e`** — **`main`** **`23677979909`** (**success**). |
| **M12** | Branch coverage baseline + separate line/branch floors | **Complete** | 2026-03-27 | Second substantive **Phase C** slice: **`.coveragerc`** **`branch = True`**; **`report:fail_under`** **removed** (coverage.py’s built-in fail-under is a **combined** line+branch ratio when both are measured — **not** the M11 **statement**-only gate); **`scripts/check_coverage_thresholds.py`** + **`tests/test_check_coverage_thresholds.py`**; **`ci` / `repo-safety`** runs **`coverage json`** then threshold script; **`DEVELOPMENT.md`**; **stdlib `unittest`** only; **no** `pytest`; **no** `mediapipe/`; **no** `VisionTaskBase` / `AudioTaskBase` extraction. | **PR #13** → `main` (**merge `3bf18cf5acad9d8ca2df97cc4f3958cbe2a11900`**, merged **2026-03-28T05:57:13Z** UTC; **`gh pr merge 13 --merge --admin`**). **PR head (pre-merge):** **`f2addfc3abf54e851801f17b2ddf3958efdc9a46`**. **Primary implementation:** **`f2addfc3abf54e851801f17b2ddf3958efdc9a46`**. **`ci` / `repo-safety`:** PR head **`23678773276`** (`push`), **`23678774673`** (`pull_request`) — **success**. Post-merge **`main`** merge push **`23678868568`** — **success** (head **`3bf18cf5acad9d8ca2df97cc4f3958cbe2a11900`**). **Proves:** **`src/aurora/`** branch coverage **measured**; **line** and **branch** floors **enforced separately** from **`coverage.json`** (**100%** statements; **100%** on **4** measured branch arcs, all in **`SharedLibraryLoader.shared_library`**). **Does not prove:** branch exhaustiveness for constructs coverage does not arc (**e.g.** some **`try`/`except`** in **`image.py`** in coverage **7.6**); governance **`scripts/`** coverage; MediaPipe/native behavior. **Closeout recorded (UTC):** **2026-03-28T05:57:13Z** (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`3bf18cf5acad9d8ca2df97cc4f3958cbe2a11900`**. **Post-closeout project record on `main`:** **`d05b383f25d0c4448555d8ff7089648a85e0b1f6`** — **`main`** **`23678888585`** (**success**). **Record alignment on `main`:** **`3ebdabdb74227b2e70611625434c5ef1a83fdd24`** — **`main`** **`23678896471`** (**success**). **Ledger enumeration on `main`:** **`828649ef1810057003e50fc255fd0734c6e4f186`** — **`main`** **`23678904630`** (**success**). **Last fully enumerated `main` tip (M12 closeout):** **`828649ef1810057003e50fc255fd0734c6e4f186`** (**`main`** **`23678904630`**). **Post-closeout record alignment:** **`afec182d6b502fe66034a09a4e040853aa4e5797`** — **`main`** **`23678916754`** (**success**). |
| **M13** | Runtime surface coherence (internal error base + `AuroraImage` DRY) | **Complete** | 2026-03-28 | Third substantive **Phase C** slice: **`src/aurora/runtime/errors.py`** — **`AuroraRuntimeError`**; **`SharedLibraryLoadError`** / **`ImageCreationError`** subclass base (constructors/messages/chaining preserved); **`AuroraImage._from_dispatch`** (private); **`verify_repo_state.py`** requires **`errors.py`**; tests + **`DEVELOPMENT.md`**; lifecycle/resource abstraction **inspected, not extracted**; **no** task bases, **no** `mediapipe/`. | **PR #14** → **`main`** (**merge `62f7128c00e78764fc69d46643b2b1473bb53f4a`**, merged **2026-03-29T00:30:32Z** UTC; **`gh pr merge 14 --merge --admin`**). **Final pre-merge PR head:** **`9df63280ef1cbf9c9f2fb214cca1b6c2b7de4340`**; **primary implementation:** **`7b83a54f335e34632a91caa3c7da3e321dd391a9`**. **`ci` / `repo-safety` (PR head):** **`23697604693`** (`push`), **`23697605208`** (`pull_request`) — **success**. Post-merge **`main`** merge push **`23697716808`** (**success**, head **`62f7128c00e78764fc69d46643b2b1473bb53f4a`**). **Closeout project record on `main`:** **`a3ffcad2d857a3750292fb331743461206ebed11`**; post-closeout **`main`** **`ci` / `repo-safety`:** **`23697727841`** (**success**). **Proves:** minimal internal error taxonomy + DRY **`AuroraImage`** creation on bounded seam; **100%** line/**100%** branch on **`src/aurora/`** maintained under existing gates. **Does not prove:** MediaPipe/native correctness; upstream parity; task bases; lifecycle/resource extraction. **Closeout recorded (UTC):** **2026-03-29T00:30:32Z** (merge time). **Last fully enumerated `main` tip (M13 closeout):** **`a3ffcad2d857a3750292fb331743461206ebed11`** (**`main`** **`23697727841`**). **Post-closeout record alignment:** **`18c6a9248904f52a02a01e9d35dcfc53c0820b4a`** — **`main`** **`23697735979`** (**success**). **Post-closeout ledger enumeration:** **`dee59693cbf45dfdf0d239b3d6a24d35d304c23d`** — **`main`** **`23697754814`** (**success**). **Post-closeout record alignment (final `main` tip after M13 closeout documentation):** **`90e51063d1fe5c1b0bfe7f18f2fe3cdb6c337b2b`** — **`main`** **`23697762788`** (**success**). |
| **M14** | Dispatch operation token centralization (`AuroraImage` dispatch literals) | **Complete** | 2026-03-29 | Fourth substantive **Phase C** slice: **`src/aurora/runtime/dispatch_tokens.py`** — **`IMAGE_FROM_FILE`**, **`IMAGE_FROM_BYTES`** (values byte-identical to pre-M14 literals); **`image.py`**, tests, **`verify_repo_state.py`** seam list, **`DEVELOPMENT.md`**; tokens **not** re-exported from **`aurora.runtime.__all__`**; **no** **`Dispatcher`** / **`LibraryLoader`** protocol change; **no** task bases, **no** `mediapipe/`. | **PR #15** → **`main`** (**merge `b9d9f52509b1b24fa87043dad635c43b15ba8cf6`**, merged **2026-03-29T01:28:21Z** UTC; **`gh pr merge 15 --merge --admin`**). **Final pre-merge PR head:** **`1e8152ec018c6fde66a63d8601e77e1240a98b04`**; **primary implementation:** **`1e8152ec018c6fde66a63d8601e77e1240a98b04`**. **`ci` / `repo-safety` (PR head):** **`23698535039`** (`push`), **`23698536291`** (`pull_request`) — **success**. Post-merge **`main`** merge push **`23698612824`** (**success**, head **`b9d9f52509b1b24fa87043dad635c43b15ba8cf6`**). **Closeout project record on `main`:** **`6e368052428acb82a86b1f30546fb0bd97e7288f`**; post-closeout **`main`** **`ci` / `repo-safety`:** **`23698632862`** (**success**). **Proves:** single source of truth for **`AuroraImage`** **`Dispatcher.dispatch`** operation tokens; value-stability unittest pins literal strings; **100%** line/**100%** branch on **`src/aurora/`** maintained. **Does not prove:** new native or decode behavior; **`VisionTaskBase`** / **`AudioTaskBase`**; lifecycle extraction; upstream Tasks migration. **Closeout recorded (UTC):** **2026-03-29T01:28:21Z** (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`b9d9f52509b1b24fa87043dad635c43b15ba8cf6`**. **Last fully enumerated `main` tip (M14 closeout documentation):** **`6e368052428acb82a86b1f30546fb0bd97e7288f`** (**`main`** **`23698632862`**). **Post-closeout record alignment (final `main` tip after M14 closeout documentation):** **`e2976391f5de1776659b4b70a2949db87510c033`** — **`main`** **`23698651000`** (**success**). **Post-closeout ledger enumeration on `main`:** **`e58fd406ba80953f7635e9f51cc31d4a05064eb3`** — **`main`** **`23698671396`** (**success**). **Final project record alignment on `main`:** **`358bbffe04c70f567066d1306efa71b80d73dc0c`** — **`main`** **`23698690645`** (**success**). |
| **M15** | `AuroraImage` dispatch invocation contract centralization (`image_dispatch.py`) | **Complete** | 2026-03-29 | Fifth substantive **Phase C** slice: **`src/aurora/runtime/image_dispatch.py`** — **`dispatch_image_from_file`** / **`dispatch_image_from_bytes`** (shared-library handle + **`Dispatcher.dispatch`** with M14 tokens and positional order **`(token, payload, lib)`**); **`image.py`** retains **`ImageCreationError`** wrapping and **`AuroraImage`** construction; **`verify_repo_state.py`** seam list; tests; **`DEVELOPMENT.md`**; helpers **not** re-exported from **`aurora.runtime.__all__`**; **no** **`Dispatcher`** / **`LibraryLoader`** protocol change; **no** task bases, **no** `mediapipe/`. | **PR #16** → **`main`** (**merge `4d694b349814357b0eaba56daff6f277680cf0c0`**, merged **2026-03-29T02:22:29Z** UTC; **`gh pr merge 16 --merge --admin`**). **Final pre-merge PR head:** **`66bd2400c572bee3703a17aa0ab8a598c08c5802`**; **primary implementation:** **`66bd2400c572bee3703a17aa0ab8a598c08c5802`**. **`ci` / `repo-safety` (PR head):** **`23699255312`** (`push`), **`23699257020`** (`pull_request`) — **success**. Post-merge **`main`** merge push **`23699450123`** (**success**, head **`4d694b349814357b0eaba56daff6f277680cf0c0`**). **Proves:** single internal module owns **`AuroraImage`** dispatch **invocation** contract (tokens from M14; frozen call shape); **100%** line/**100%** branch on **`src/aurora/`** maintained. **Does not prove:** MediaPipe/native behavior or decode; **`VisionTaskBase`** / **`AudioTaskBase`**; lifecycle extraction; upstream Tasks migration. **Closeout recorded (UTC):** **2026-03-29T02:22:29Z** (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`4d694b349814357b0eaba56daff6f277680cf0c0`**. **Closeout project record on `main`:** **`3ac7ead7ccd06eee6567fed9883ecda1d4acf729`**; post-closeout **`main`** **`ci` / `repo-safety`:** **`23699467585`** (**success**). **Last enumerated `main` tip (M15 closeout documentation):** **`3b0aa82960037363e01e93daa3f1144b59906956`** (**`main`** **`23699475645`**). |
| **M16** | Phase C closeout and Phase D authorization gate | **Complete** | 2026-03-30 | **Governance only:** mark **Phase C** **closed** and **Phase D** **authorized** (planning/ingress first) in **`docs/aurora.md`**; workspace **`docs/phase_summaries/phase_c_summary.md`** (not in repo); seed **M17** stub; **no** `src/aurora/` code. **Inspection baseline `main`:** **`e228a5fec17585c41365ff00aab306ae75810a6c`**. | **PR #17** — https://github.com/m-cahill/aurora/pull/17 — **`gh pr merge 17 --merge --admin`**. **Merge commit / `main`:** **`f55ae2b96098b7648d3e3de7144e6a474b0af07c`**, merged **2026-03-31T20:06:21Z** UTC. **Final pre-merge PR head:** **`61914da76db15a6b110c15e4b0bfded7418bd121`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23780952862`** (`push`), **`23780954198`** (`pull_request`) — **success**. Post-merge **`main`** merge push **`23817061439`** (**success**, head **`f55ae2b96098b7648d3e3de7144e6a474b0af07c`**). **Closeout project record on `main`:** **`d5a610ce345673812a927355eebf40bda7a0fef8`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23817090520`** (**success**). **Ledger enumeration on `main`:** **`af25ee082ee28805f80e58c23d6513be4c88e0d2`** — post-enumeration **`main`** **`ci` / `repo-safety`:** **`23817122650`** (**success**). **Proves:** **Phase C** **closed** in the committed record; **Phase D** **authorized** for **planning/ingress/scoping** — **not** monolithic kernel implementation by default. **Does not prove:** kernel extraction; **`VisionTaskBase`/`AudioTaskBase`**; MediaPipe/native correctness; upstream Tasks migration. **Changed:** **`docs/aurora.md`** only (phase state + ledger + closeout evidence). **Did not change:** **`src/aurora/`**; **CI** workflow identity or coverage thresholds. **Closeout recorded (UTC):** **2026-03-31T20:06:21Z** (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`f55ae2b96098b7648d3e3de7144e6a474b0af07c`**. **Last fully enumerated `main` tip / ledger-enumeration commit (M16 closeout documentation):** **`af25ee082ee28805f80e58c23d6513be4c88e0d2`** (**`main`** **`23817122650`**). **Authoritative final post-closeout `main` tip / phase-record tip (M16):** **`288a095a1a764ccd5b6c899cab63ce68e3929eb4`** (**`main`** **`ci` / `repo-safety`:** **`23817152004`**, **success**). |
| **M17** | Phase D entry: kernel ingress framing and source-of-truth alignment | **Complete** | 2026-03-31 | **Docs-only:** **`docs/kernel_ingress_strategy.md`** (new) + **`docs/aurora.md`** (M16 **SHA** roles, Phase D constraints, roadmap **IV** note, **M17** handoff); **no** `src/aurora/` code; **no** CI/workflow/threshold edits. | **PR #18** — https://github.com/m-cahill/aurora/pull/18 — **`gh pr merge 18 --merge --admin`**. **Merge commit:** **`a937346606bf3d600db1a3e15f13dc7d34162be9`**, merged **2026-04-01T00:12:34Z** UTC. **Final pre-merge PR head:** **`07295d120d58bb1031f8080c44ffb95e3a0ddb47`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23821415583`** (`push`, **success**), **`23823785524`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23825434422`** (**success**, head **`a937346606bf3d600db1a3e15f13dc7d34162be9`**). **Proves:** **Phase D** entry **framing** in-repo; **Phase D** remains **planning / ingress / scoping** — **not** kernel implementation. **Does not prove:** acoustic kernel extraction; **`VisionTaskBase`/`AudioTaskBase`**; MediaPipe/native correctness. **Changed (PR):** **`docs/aurora.md`**, **`docs/kernel_ingress_strategy.md`**. **Did not change:** **`src/aurora/`**; workflow identity or thresholds. **Closeout recorded (UTC):** **2026-04-01T00:12:34Z** (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`a937346606bf3d600db1a3e15f13dc7d34162be9`**. **Closeout project record on `main`:** **`14647129faa746cf37be3fb2c0e1e390fe4d0623`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23825459937`** (**success**). **Record alignment on `main`:** **`46b4c22820ac1fe4a73b0340a41959a7b03b505b`** — **`main`** **`ci` / `repo-safety`:** **`23825472539`** (**success**). **Last fully enumerated `main` tip (M17 closeout documentation):** **`14647129faa746cf37be3fb2c0e1e390fe4d0623`** (**`main`** **`23825459937`**). **Record alignment on `main`:** **`46b4c22820ac1fe4a73b0340a41959a7b03b505b`** (**`main`** **`23825472539`**). **Authoritative final post-closeout `main` tip / phase-record tip (M17):** **`19a23346920bae1cb6b677fcacceb50b72bb2dac`** (**`main`** **`23825505075`**). **Phase-record SHA label commit (M17 closeout chain):** **`1cb7119e86f89e25a9282abf6514e44e014ebbb5`** (**`main`** **`23825524515`**). **Final `main` tip after M17 closeout documentation (preceding commit):** **`1fd57b48341b0985cb98a5808abc3a1188cc11e2`** (**`main`** **`23825582016`**). |
| **M18** | Phase D ingress inventory and first kernel-slice target lock | **Complete** | 2026-03-31 | **Docs-only:** **`docs/acoustic_kernel_surface_inventory.md`** (Python+C++ audio path, functional groups, **`@com_google_audio_tools`** dependency map, per-calculator appendix, M19 candidates **A** / **B** / **C** with **Candidate A** recommended); **`docs/aurora.md`** alignment; **no** `src/aurora/`; **no** CI/workflow/threshold edits; **`DEVELOPMENT.md`** unchanged. | **PR #19** — https://github.com/m-cahill/aurora/pull/19 — **`gh pr merge 19 --merge --admin`**. **Merge commit:** **`7f12f0cbaedb9daa4cf3a2114f7d1210aff7b74f`**, merged **2026-04-01T02:35:02Z** UTC. **Final pre-merge PR head:** **`75720b21db446023063037504c19e0c2f0997289`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23827588371`** (`push`, **success**), **`23827590923`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23829170430`** (**success**, head **`7f12f0cbaedb9daa4cf3a2114f7d1210aff7b74f`**). **Proves:** evidence-backed **bounded** Phase D ingress inventory and **M19** slice recommendation; **Phase D** remains **planning / ingress / scoping** — **not** kernel implementation. **Does not prove:** kernel extraction; MediaPipe/native/decode correctness; **`VisionTaskBase`/`AudioTaskBase`**. **Changed (PR):** **`docs/aurora.md`**, **`docs/acoustic_kernel_surface_inventory.md`**. **Did not change:** **`src/aurora/`**; workflow identity or thresholds. **Closeout recorded (UTC):** **2026-04-01T02:35:02Z** (merge time). **Authoritative `main` tip immediately after merge (merge commit):** **`7f12f0cbaedb9daa4cf3a2114f7d1210aff7b74f`**. **Closeout project record on `main`:** **`d468ab7eacc70683d7ecf336eda097f79da9f909`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23829193823`** (**success**). **Last enumerated `main` tip (M18 closeout documentation):** **`d468ab7eacc70683d7ecf336eda097f79da9f909`** (**`main`** **`23829193823`**). **Ledger enumeration on `main`:** **`5d75bca6ffd71398d6060817bc70d67efcd82cf7`** — post-enumeration **`main`** **`ci` / `repo-safety`:** **`23829211160`** (**success**). **Authoritative final post-closeout `main` tip / phase-record tip (M18):** **`5d75bca6ffd71398d6060817bc70d67efcd82cf7`** (**`main`** **`23829211160`**). **Final `main` tip after M18 closeout documentation (record alignment):** **`93810468acf6431c32aee7ec2a1799e4350062d7`** — **`ci` / `repo-safety`:** **`23829229685`** (**success**). **Last fully enumerated `main` tip / ledger-enumeration commit (M18 closeout documentation):** **`40fe6c2c0c8660089942cb0bcb85fa5fa3251aee`** (**`main`** **`23829245277`**). **Authoritative final enumerated `main` tip (M18 closeout chain):** **`27a963b827a0c8e819bf3b3490049597aa519669`** (**`main`** **`23829260279`**). **Final `main` tip after M18 closeout documentation (project record):** **`1331e4c9a4017ad739747ab65b41a7d62cf29f02`** — **`ci` / `repo-safety`:** **`23829656697`** (**success**). |
| **M19** | Bounded Python-first audio seam (Candidate A — `AuroraAudio`) | **Complete** | 2026-03-31 | First **Phase D** **code** milestone: **`src/aurora/runtime/audio.py`** (**`AuroraAudio`**, **`AudioCreationError`**), **`audio_dispatch.py`**, **`AUDIO_FROM_FILE`** / **`AUDIO_FROM_BYTES`** in **`dispatch_tokens.py`**; **`tests/test_audio_runtime.py`**; audio composition in **`tests/test_runtime_smoke.py`**; **`verify_repo_state.py`** seam list; **`DEVELOPMENT.md`**; **`docs/acoustic_kernel_surface_inventory.md`**, **`docs/kernel_ingress_strategy.md`** — **no** C++/BUILD/TFLite/graph, **no** `mediapipe/` copy, **no** CI workflow identity or coverage threshold changes (seam file enumeration only). | **PR #20** — https://github.com/m-cahill/aurora/pull/20 — **`gh pr merge 20 --merge --admin`**. **Merge commit:** **`4ee7570468a6dc63a7247ae1213783284b0e0ffb`**, merged **2026-04-01T04:12:20Z** UTC. **Final pre-merge PR head:** **`dd38c88cd135661eddf1a7c66015db9c7cb47e5e`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23831031795`** (`push`, **success**), **`23831144867`** (`pull_request`, **success**). Post-merge **`main`** push **`23831552846`** (**success**, head **`4ee7570468a6dc63a7247ae1213783284b0e0ffb`**). **Proves:** additive bounded **offline / deterministic** Python seam parallel to **`AuroraImage`**; fake-backed structural tests. **Does not prove:** acoustic kernel extraction; MediaPipe/native/decode correctness; **`@com_google_audio_tools`** in **`aurora/`**; **`VisionTaskBase`/`AudioTaskBase`**. **Authoritative `main` tip immediately after merge (merge commit):** **`4ee7570468a6dc63a7247ae1213783284b0e0ffb`**. **Closeout project record on `main`:** **`b05ca1bba51c95d70fb721906547c17a9d4a1f9c`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23831588681`** (**success**). **Project-record enumeration on `main`:** **`39a6f7230ad567da30829023e3f611136a17d5e2`** — post-enumeration **`main`** **`ci` / `repo-safety`:** **`23831613586`** (**success**). **Authoritative final enumerated `main` tip (M19 closeout):** **`00e8bf57f08d56c3381b81273c0b942086e20aee`** — **`main`** **`ci` / `repo-safety`:** **`23831631106`** (**success**). **Closeout recorded (UTC):** **2026-04-01T04:12:20Z** (merge time). |
| **M20** | Audio classifier graph mapping and native-wiring target lock | **Complete** | 2026-03-31 | **Docs-only** (**Candidate B**-style): **`docs/audio_classifier_graph_mapping.md`** (new); updates **`docs/acoustic_kernel_surface_inventory.md`**, **`docs/kernel_ingress_strategy.md`**, **`docs/aurora.md`** — evidence-backed map of upstream **AudioClassifier** Python → C → C++ graph (**`AudioToTensorCalculator`**, inference, post-processing); **BUILD** vs **`audio_classifier_graph.cc`** nuance for **`time_series_framer_calculator`**; locks proof bars / non-goals for future **D1** wiring — **no** C++/BUILD/TFLite in **`aurora/`**, **no** `mediapipe/` copy, **no** `src/aurora/` runtime behavior change by default, **no** native/MediaPipe correctness claims. | **PR #21** — https://github.com/m-cahill/aurora/pull/21 — **`gh pr merge 21 --merge --admin`**. **Merge commit:** **`c7e9de3da82e81ac697f73ddde55708151f6529c`**, merged **2026-04-01T18:25:07Z** UTC. **Final pre-merge PR head:** **`c59f347345220764f8d7b49e5bbfd2631ff65c5d`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23834691546`** (`push`, **success**), **`23834692922`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23864205704`** (**success**, head **`c7e9de3da82e81ac697f73ddde55708151f6529c`**). **Proves:** committed **descriptive** upstream graph mapping + dependency pressure naming; **BUILD** vs graph-**`.cc`** distinction for framer. **Does not prove:** native execution, decode, Tasks parity, or **D1** wiring. **Authoritative `main` tip immediately after merge (merge commit):** **`c7e9de3da82e81ac697f73ddde55708151f6529c`**. **Closeout project record on `main`:** **`517bdb6a1e6fc2cd7c25d2dd50af363f20f490fe`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23864253198`** (**success**). **Authoritative final enumerated `main` tip (M20 closeout documentation):** **`517bdb6a1e6fc2cd7c25d2dd50af363f20f490fe`** (**`main`** **`23864253198`**). **Closeout recorded (UTC):** **2026-04-01T18:25:07Z** (merge time). |
| **M21** | Bounded D1 native **`AUDIO_FROM_FILE`** ingress | **Complete** | 2026-04-01 | **`audio_native_bindings.py`**, **`NativeAudioDispatcher`** — ctypes **`MpAudioClassifier*`** + **`Dispatcher.dispatch`** for **`AUDIO_FROM_FILE`**; model path on dispatcher; at M21 ship **`AUDIO_FROM_BYTES`** on **`NativeAudioDispatcher`** was **not** yet wired (**M22** completed bytes path). Tests fake-backed — **structural** proof only. **No** C++/BUILD in **`aurora/`**, **no** `mediapipe/` copy, **no** framer-as-node claim vs **M20**. | **PR #22** — https://github.com/m-cahill/aurora/pull/22 — **`gh pr merge 22 --merge --admin`**. **Merge commit:** **`12c3e91bd097c0e4a9c9a1ca00862bf9a6d9badf`**, merged **2026-04-01T20:08:58Z** UTC. **Final pre-merge PR head:** **`43f2e1f5b3885644d5c9b21955eddce6cfa97594`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23868418397`** (`push`, **success**), **`23868465430`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23868499158`** (**success**, head **`12c3e91bd097c0e4a9c9a1ca00862bf9a6d9badf`**). **Proves:** **structural** ctypes binding + **`NativeAudioDispatcher`** dispatch path for **`AUDIO_FROM_FILE`**. **Does not prove:** decode correctness, graph execution, Tasks parity. **Authoritative `main` tip immediately after merge (merge commit):** **`12c3e91bd097c0e4a9c9a1ca00862bf9a6d9badf`**. **Closeout project record on `main`:** **`5385d76b75ad84354a1b9514c81363e4d88cf962`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23868561637`** (**success**). **Project-record enumeration on `main`:** **`ca3ff0b221a5cd2a1c2066809d93df48b8c700cd`** — post-enumeration **`main`** **`ci` / `repo-safety`:** **`23868610524`** (**success**). **Record alignment on `main`:** **`c2e29ba58d24de8c2d66d5ee5bf0fc6f63ac1180`** — post-alignment **`main`** **`ci` / `repo-safety`:** **`23868658916`** (**success**). **Authoritative final enumerated `main` tip (M21 closeout documentation):** **`c2e29ba58d24de8c2d66d5ee5bf0fc6f63ac1180`** — **`main`** **`ci` / `repo-safety`:** **`23868658916`** (**success**). **Closeout recorded (UTC):** **2026-04-01T20:08:58Z** (merge time). |
| **M22** | Bounded D1 native **`AUDIO_FROM_BYTES`** on **`NativeAudioDispatcher`** | **Complete** | 2026-04-01 | **`native_audio_dispatcher.py`** — **`AUDIO_FROM_BYTES`** same lifecycle as **`AUDIO_FROM_FILE`**; **`MpAudioData`** from **`bytes`** (structural float32 lanes — **not** decode); **`AudioNativeBytesDeferredError`** removed; tests + **`DEVELOPMENT.md`**, **`kernel_ingress_strategy.md`** §10, **`runtime_seam_framing.md`** §6d. **No** C++/BUILD in **`aurora/`**, **no** `mediapipe/` copy. **Local-native proof:** **not attempted** (no suitable library in scope — workspace note). | **PR #23** — https://github.com/m-cahill/aurora/pull/23 — **`gh pr merge 23 --merge --admin`**. **Merge commit:** **`19859006a13eafc7978694ba5e14a61829573f17`**, merged **2026-04-01T23:12:12Z** UTC. **Final pre-merge PR head:** **`70b1bde2dd34b225ae212abb5d81b0568c1d3560`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23872381169`** (`push`, **success**), **`23872383778`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23875372450`** (**success**, head **`19859006a13eafc7978694ba5e14a61829573f17`**). **Proves:** **structural** dispatch path for **`AUDIO_FROM_BYTES`**; frozen **`dispatch(token, payload, lib)`** seam. **Does not prove:** decode correctness, graph/TFLite correctness, MediaPipe parity, or real native execution in CI. **Authoritative `main` tip immediately after merge (merge commit):** **`19859006a13eafc7978694ba5e14a61829573f17`**. **Closeout project record on `main`:** **`fad5ffd30037f7f8e355f56f45b7322d09258c13`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23875409786`** (**success**). **Record alignment on `main`:** **`e4c7c5edcd83327328fcccf5f7cd0e3a6fc5e324`** — post-alignment **`main`** **`ci` / `repo-safety`:** **`23875427533`** (**success**). **Authoritative final enumerated `main` tip (M22 closeout chain):** **`e4c7c5edcd83327328fcccf5f7cd0e3a6fc5e324`** — **`main`** **`ci` / `repo-safety`:** **`23875427533`** (**success**). **Closeout recorded (UTC):** **2026-04-01T23:12:12Z** (merge time). |
| **M23** | Phase D governance closeout | **Complete** | 2026-04-01 | **Governance only:** **`docs/aurora.md`** — Phase D **closed**; **closure criteria** + **closure matrix**; explicit non-proofs (**local-native** **not attempted**); **`docs/kernel_ingress_strategy.md`** §11; **`docs/runtime_seam_framing.md`** pointer; workspace **`docs/phase_summaries/phase_d_summary.md`**; **M24** stub; **no** `src/aurora/` by default. | **PR #24** — https://github.com/m-cahill/aurora/pull/24 — **`gh pr merge 24 --merge --admin`**. **Merge commit:** **`64cc610d690e61edb026c37ec7cd1915a1fbba89`**, merged **2026-04-02T00:23:40Z** UTC. **Final pre-merge PR head:** **`8de685a729d4bdd9d1d131fc9b5c683809e84929`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23876960921`** (`push`, **success**), **`23876961935`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23877456336`** (**success**, head **`64cc610d690e61edb026c37ec7cd1915a1fbba89`**). **Proves:** committed **governance** closure under **Interpretation B** fallback; honest record of **structural** Phase D scope. **Does not prove:** decode, graph, parity, native execution, or that **local-native** proof occurred. **Authoritative `main` tip immediately after merge (merge commit):** **`64cc610d690e61edb026c37ec7cd1915a1fbba89`**. **Closeout project record on `main`:** **`9d8296d449c42a43a505f539a0736a4b0a213de4`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23877506186`** (**success**). **Authoritative final enumerated `main` tip (M23 closeout project record):** **`9d8296d449c42a43a505f539a0736a4b0a213de4`** — **`main`** **`ci` / `repo-safety`:** **`23877506186`** (**success**). **Closeout recorded (UTC):** **2026-04-02T00:23:40Z** (merge time). |
| **M24** | Phase E readiness audit + ecosystem boundary charter | **Complete** | 2026-04-02 | **Governance only:** Phase E **readiness audit** (workspace **`docs/milestones/M24/phase_e_readiness_audit.md`**); **`docs/aurora.md`** — **Proposed downstream repo layout and dependency directionality**; **M25** stub; **no** formal **post–Phase D** execution-phase label canonized by default; **no** `src/aurora/`; **no** `mediapipe/`; **no** ORNITHOS/PANTANAL-1 repo creation. | **PR** **#25** — https://github.com/m-cahill/aurora/pull/25 — **`gh pr merge 25 --merge --admin`**. **Merge commit:** **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`**, merged **2026-04-02T01:41:38Z** UTC. **Final pre-merge PR head:** **`f9d9fbe3603d10402023b42896efb9d96e10e4d0`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23879281514`** (`push`, **success**), **`23879282695`** (`pull_request`, **success**). Post-merge **`main`** merge push **`23879539276`** (**success**, head **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`**). **Proves:** downstream **roles** + **one-way** **dependency** **charter** in committed record; **honest** **non-proofs** preserved. **Does not prove:** decode, graph, parity, native execution, ORNITHOS/PANTANAL-1 repos, competition implementation. **Authoritative `main` tip immediately after merge (merge commit):** **`960bee3e0b9470c2b0b7e47c6a63af427bca1484`**. **Closeout project record on `main`:** **`21985bf488b67ccf4c62ba2fcaeac16c1d6429cd`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23879695944`** (**success**). **Authoritative final enumerated `main` tip (M24 closeout project record):** **`21985bf488b67ccf4c62ba2fcaeac16c1d6429cd`**. **Closeout recorded (UTC):** **2026-04-02T01:41:38Z** (merge time). |
| **M25** | ARB boundary framing and minimal run-bundle specification | **Complete** | 2026-04-01 | **Docs/governance only:** **`docs/aurora_run_bundle_boundary.md`** (new) + **`docs/aurora.md`** + **`scripts/verify_repo_state.py`** + **`tests/test_verify_repo_state.py`** + **`DEVELOPMENT.md`**; **no** `src/aurora/`; **no** ARB implementation; **no** new **Execution Phase Boundaries (locked)** row. | **PR** **#26** — https://github.com/m-cahill/aurora/pull/26 — **`gh pr merge 26 --merge --admin`**. **Merge commit:** **`4037db3b0c037efea533e1a353565c9f70139a14`**, merged **2026-04-02T02:57:26Z** UTC. **Final pre-merge PR head:** **`6eeac778651e5cd7c92d4d97f526ee8cbca186a8`**. **Authoritative PR-head `ci` / `repo-safety`:** **`23881492177`** (`push`, **success**), **`23881499188`** (`pull_request`, **success**). Post-merge **`main`** **`23881508861`** (**success**, head **`4037db3b0c037efea533e1a353565c9f70139a14`**). **Proves:** committed **ARB** **boundary/spec**; **non-canonization** of **post–Phase D** execution-phase label. **Does not prove:** ARB **implementation**; decode, graph, parity, native execution; competition readiness. **Authoritative `main` tip immediately after merge (merge commit):** **`4037db3b0c037efea533e1a353565c9f70139a14`**. **M25** **closeout project record on `main`:** **`12fac02dcd9e88fea72e9c5e3da11c3da585b12b`** — post-closeout **`main`** **`ci` / `repo-safety`:** **`23881539057`** (**success**). **Authoritative final enumerated `main` tip (M25 closeout project record):** **`12fac02dcd9e88fea72e9c5e3da11c3da585b12b`**. **Closeout recorded (UTC):** **2026-04-02T02:58:35Z**. |

---

## References (workspace; not committed here)

Committed strategy and record in this repo:

- `docs/runtime_surface_strategy.md` — **tracked runtime surface ingress, layout, Phase B entry contract** (M04; layout executed M05); **Phase B closeout** §6a (**M10**)
- `docs/runtime_substrate.md` — **first-party substrate path, modules, provenance** (M05)
- `docs/runtime_seam_framing.md` — **Phase B seam framing: `Dispatcher` / `LibraryLoader`, M07 `SharedLibraryLoader`, M08 bounded `AuroraImage`, M09 composed smoke, LIVE_STREAM honesty** (M06–M09); **M19** bounded **`AuroraAudio`** (parallel seam — see **`DEVELOPMENT.md`**); **M21** / **M22** **`NativeAudioDispatcher`** (bounded D1 — **`AUDIO_FROM_FILE`** / **`AUDIO_FROM_BYTES`**)
- `docs/kernel_ingress_strategy.md` — **Phase D entry framing** (M17); **M23** Phase D closeout (§11)
- `docs/acoustic_kernel_surface_inventory.md` — **Phase D: Python+C++ audio surface inventory, `@com_google_audio_tools` map, M19 slice candidates** (M18 inventory; **M19** implements Candidate **A** only — bounded Python seam)
- `docs/audio_classifier_graph_mapping.md` — **M20** — upstream **AudioClassifier** graph mapping and native-wiring target lock (evidence-backed; **no** runtime proof)
- `docs/aurora_run_bundle_boundary.md` — **M25** — **ARB** boundary and **v0** conceptual run-bundle shape (**boundary/spec only** — **not** ARB implementation)

Authoritative narrative and evidence live outside this repo in the workspace, for example:

- `docs/phase_summaries/phase_c_summary.md` — **Phase C** closeout summary (**M16**); workspace governance (not committed in `aurora/`)
- `docs/phase_summaries/phase_d_summary.md` — **Phase D** closeout summary (**M23**); workspace governance (not committed in `aurora/`)
- `docs/milestones/M24/phase_e_readiness_audit.md` — **M24** Phase E readiness audit (workspace governance; not committed in `aurora/`)
- `docs/context/AURORA_VISION.md`
- `docs/context/mediapiperefactoraudit.md`, `docs/context/mediapipefinishingaudit.md`
- `docs/context/PANTANAL_1_MOONSHOT.md`
- `docs/preflight/aurora_startup_lock/` (startup locks and phase boundaries)

When those documents change, **this file should be updated at milestone closeout** if the repo-level posture or roadmap needs to stay aligned.

